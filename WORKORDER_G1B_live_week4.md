# WORK ORDER — 4주차: G1B 라이브 구축 → 드라이런 → 60일 log-only 개시

- 발주 문서 버전: v1.0
- 대상: Claude Code (`g1br/` 확장 또는 `g1live/` 신설 — 에이전트 판단, 근거 보고)
- 정본: {SPEC_G1B v0.3 + SPEC_G1_OPT v0.2 + **SPEC_AMENDMENT_A1**}. 충돌 시 A1 우선.
- 목표: G1B 라이브 파이프라인 가동 → **3일 드라이런 통과 = 게이트 D+0** → 60일 log-only 개시.
  전 기간 자본 무투입 (log-only). D+15에 G0→G1 판정 재료가 자동으로 쌓여 있어야 한다.

## 1. T1 — 라이브 데이터 수집기

- 야간 배치 (06:00~07:15): SPX·SOXX·MU·NVDA 정규 종가 / 미 시간외 바스켓 / 글로벡스 스냅샷 /
  GDR(SMSN.IL) / 야간선물·TAIFEX (조달 가능 소스 재탐색 — 라이브는 당일치만 필요하므로
  히스토리 불가 소스도 후보, 조사 상한 소스당 2시간) / ^TNX·ZT 감시 컬럼.
- 아침 배치 (08:00~08:45): ASX·니케이 선물 조기 / 글로벡스 2차 / KRX 동시호가 예상체결가
  (증권사 API 가용성 실측 — 불가 시 "R2 잔차 판정 보류·관측 결측" 플래그로 정직 운영).
- 모든 수집에 `fetch_ts` 기록 + **07:15/08:45 절단 강제** (절단 후 도착 데이터는 당일 사용 금지,
  `late_arrival` 플래그로 별도 기록 — 라이브판 룩어헤드 방지).
- 결측 처리: 소스별 결측 시 해당 항 가중 0 재정규화 + 리포트에 결측 표기. 파이프라인 정지 금지.

## 2. T2 — FairGap 라이브 엔진

- pack_v1.0 로드 (A1-3의 w_reg 하향 보정 반영: 삼전 0.4 / 하닉 0.6). unverified 가중 항은
  데이터 확보 시에만 활성, 미확보 시 자동 재정규화.
- 일간 학습 상태 계층 가동 (G1-OPT §4): 칼만 β(클램프)·EWMA σ(λ 0.94)·bias 보정(클램프 0.3σ)·
  Hedge 가중(전문가: reg / GDR / gx / [삼전 한정 B1] — A1-5)·케이스 메모리 적재(가중 0 출발).
- σ 이중 체계 (A1-4): 중심 판정 σ 배수 / 꼬리 판정 경험 분위수 병행 산출.
- AdjEx·레짐 분류(이벤트 캘린더 전방 수집)·ZT 감시 자동.

## 3. T3 — R1/R2 리포트 발행기

- R1 07:20 · R2 08:55 정시 발행 (G1B v0.3 §10 템플릿). log-only 표기 필수: 조정 지시는
  "가상 지시"로 명기 (A1-6: Phase 1 활성화는 D+15 게이트 통과 후).
- G1A 스냅샷 로그와 date 조인 — G1A 가상 판정이 있으면 잔여기대 σ 환산 포함.
- 층별 절제 병행 기록 (reg 단독 / +관측 / +δ) + B1·B2 병행 — 게이트·절제 판정 재료.

## 4. T4 — 게이트 계기판 (D+15 자동 판정 재료)

- 일간 자동 집계: 가동률(정시 발행), translation_error_r1/r2 (중앙값 롤링), 오프라인 예측 대비
  1.5배 정합 여부, 룩어헤드·late_arrival 카운트, σ 커버리지·PIT, 결측 소스 일지.
- `gate_dashboard.md` 매일 갱신 — D+15에 사람이 집계하는 게 아니라 이미 판정표가 놓여 있게.
- 킬스위치·건강 경보는 log-only 기간에도 **가상 발동 기록** (실자본 없으니 조치는 없지만
  발동 이력 자체가 60일 검증 대상).

## 5. T5 — 3일 드라이런 → D+0 선언

- 연속 3영업일: R1/R2 정시 발행 + 치명 오류 0 + 절단 위반 0 → **드라이런 통과 보고와 함께
  게이트 D+0 선언** (A1-6). 실패 시 원인 수정 후 3일 재시작.
- D+0 선언 보고에 포함: 확보된 라이브 소스 최종 목록 (야간선물·시간외·동시호가 성패),
  그에 따른 활성 가중 구성, 60일 이관 목록 최종판.

## 6. 운영 수칙 (60일 기간)

- 파라미터·가중 수동 변경 금지 (상태 계층의 자동 갱신만). fx 챌린저 섀도는 G1-OPT §6 절차로
  별도 등재 후 가동 (가설 사전 등록 필수 — A1-3).
- δ_manual 입력은 발주자만, Q1 태그+사유 강제 (시스템 접수 거부 로직 포함).
- 주간 보고: gate_dashboard 요약 + 전문가 리더보드 + 이상치 밤 포스트모템 초안.

## 7. DoD

- [ ] T1~T4 가동, 드라이런 통과, **D+0 선언 보고**
- [ ] 라이브 소스 성패 최종표 + 활성 가중 구성
- [ ] gate_dashboard 자동 갱신 확인 (첫 3일치 첨부)
- [ ] fx 챌린저 사전 등록 문서

## 8. 하지 말 것

- 실자본 연동 일체 (60일 전 기간 log-only) / 관문·임계 수치 변경 / 스펙·A1 수정
- 히스토리 재조달 재시도 (라이브 당일치 수집만) / D+15 판정 자체 (판정은 발주자)
