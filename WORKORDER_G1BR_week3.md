# WORK ORDER — G1B-R 구현 3주차: 진단·총검증·가중 역산·param_pack_v1

- 발주 문서 버전: v1.0
- 대상: Claude Code (`g1br/`, 2주차 산출물 위에서 진행)
- 상위 스펙: SPEC_G1B_R v0.2 §3.4~3.5·§5~7, SPEC_G1B v0.3 §3~4, SPEC_G1_OPT v0.2 (§6 초대 챔피언 연계)
- 작업 규칙 5개조 유지. 본 주의 산출물 **param_pack_v1.json이 초대 챔피언(pack_v1.0)** 이 된다.

## 0. 2주차 보고에 대한 발주자 판정 (본 절이 스펙·직전 계획에 우선)

| # | 쟁점 | 판정 |
|---|---|---|
| J1 | 사전 등록 어긋남 ①② | **연결 가설 채택**: β_mkt 과대 → 고유 라벨 오염 가능성. T1~T2 진단으로 원인 특정. 예상에 맞춘 수동 조정 금지 — β_mkt 선택은 결합 OOS 오차가 결정 |
| J2 | σ 표본 선택 | **긴 표본 확정**: σ_base는 전 기간 I1a 기반 결합 잔차로 추정. 챔피언이 I2b여도 σ 뼈대는 유지, 중첩 구간 스케일 배율 보정만 (배율 상한 1.3) |
| J3 | 대변동 표본 3밤 | 기준(±3%) **재정의 금지**. σ_base(대변동) = "미검증·60일 이관" 표기 + 잠정 규칙 σ(대변동)=1.5×σ(이벤트). \|SPX\|≥2%는 레짐이 아닌 보조 진단으로만 산출 |
| J4 | 베이스라인 대체 | 야간선물 베이스라인 사용 불능 → **B1: SOXX 단독 M0(종목갭 직접), B2: SPX 단독 지수 매핑(β_mkt·b1·r_SPX)** 2종으로 대체 |

## 1. T1 — β_mkt 진단과 선택 (J1)

1. β_mkt 4안 산출 (삼전·하닉 각각): (i) 현행 롤링 120일 / (ii) 대변동 밤(\|SPX\|≥3%) 제외 롤링 / (iii) Huber 강건 회귀 / (iv) 고정 1.2 (사전 등록 중앙값).
2. 진단 차트: 롤링 β 시계열 4안 중첩, 서브샘플(반기별) 안정성 표. β 급등이 소수 극단 밤 기여인지 분해.
3. **선택 규칙**: 4안 각각으로 고유 라벨을 재생성해 T4 총검증에 태우고, 결합 FairGap의 OOS translation_error 최소안 채택. 동률(차이 < 부트스트랩 80%)이면 (iii) Huber 우선(구조적 보수성).

## 2. T2 — 채널 기여 재진단 (J1)

1. **비교 함정 명시**: 지수 R²(0.27)와 고유 R²(0.06)는 라벨이 달라 직접 비교 불가 — WEEK3_REPORT에 두 채널의 기여를 **결합 예측의 오차 감소분(절제 방식)** 으로 재표현: {B2 → +지수 회귀 → +고유 채널} 단계별 OOS 오차.
2. 앵커 회귀: 원시 종목갭 ~ r_SOXX 단독 (분해·직교화 없음) — 반도체 신호의 총량 확인용 기준점.
3. T1의 β_mkt 4안별 고유 채널 R²·오차 재산출 — "β 교정 시 고유 기여 회복" 가설의 직접 검정.
4. 결론을 사전 등록 대조표에 기입: 어긋남 ①이 (a) β 오염이었는지 (b) 진짜 시장주도 구조인지 판정. (b)로 판명 시 그것을 그대로 기록 — G1B 라이브에서 고유 채널 가중이 Hedge에 의해 자연 축소될 것이므로 설계 변경 불요.

## 3. T3 — σ_base 확정 (J2·J3)

1. 전 기간 I1a 기반 결합 잔차(T1 채택 β_mkt 적용)로 레짐별 σ_base 재추정: 평상 / 이벤트(47밤) / 대변동(미검증 처리).
2. 이벤트/평상 비율 재산출 → 사전 등록(1.6~2.2) 대조. 범위 밖이면 수치 그대로 기록 + 해석 1문단 (강제 조정 금지).
3. J3 잠정 규칙 구현: σ(대변동)=1.5×σ(이벤트), 플래그 `provisional=true`. \|SPX\|≥2% 보조 진단 수치 병기.
4. 커버리지 검정: 1σ 65~72% / 2σ ≥93% (스펙 §1 관문). 챔피언 갈래가 I2b일 경우 중첩 구간 배율 보정 산출 (상한 1.3, 초과 시 보고·중단).

## 4. T4 — 워크포워드 총검증 (스펙 §3.5)

1. 갈래 매트릭스: {I1a, I1b(조건부 편입 시)} × {S0/S1/S2} × {β_mkt 4안} — 전 조합을 돌리지 말고 **사다리 순서로 가지치기** (하위 단계 개선 없으면 상위 중단). 최종 후보 ≤ 4개로 압축.
2. 각 후보: 07:15 재현 워크포워드 (available_by_0715 준수, AdjEx 적용, ZT 감시 컬럼 반영), translation_error 분포(전체/레짐별), 부호 적중률(\|gap\|≥0.3% 한정), 대형 갭일(≥1.5%) 별도.
3. **베이스라인 B1·B2 대조 (J4)** — 최종 후보가 B1·B2 모두를 못 이기면 §7 강등 사다리 발동, 결과 그대로 보고.
4. 건강 경보 히스토리 시뮬레이션 (5일 MAE 90분위) — 과거 발동 시점·구간 기록.
5. 다중검정 감사: 시도한 조합 수·가지치기 로그 자동 집계 (WEEK3_REPORT 필수 절).

## 5. T5 — 가중 역산 (범위 한정: 관측부 = GDR + GX)

1. 워크포워드 각 밤의 {회귀부 예측, GDR, GX} 저장 → 레짐별 볼록결합 가중 격자 탐색 (0.1 단위).
2. **2단 분할 필수**: 탐색은 검증기간 전반부만, 후반부 동결 평가 (스펙 §3.4).
3. 미검증 가중(야간선물 u1·TAIFEX u3·시간외 v2)은 param_pack에 스펙 초기값 + `unverified=true` 로 명시 — 60일 로그 검증 대상임을 팩 자체가 선언하게 한다.

## 6. T6 — param_pack_v1.json + 최종 보고

- 팩 내용: 채택 갈래·β 일체(β_mkt 포함, 추정 방식 태그), σ_base(레짐·provisional 플래그), 역산 가중(검증/미검증 구분), ZT 감시 설정, AdjEx 설정, 생성 커밋 해시.
- O8 이관 목록 완결판: 오프라인 미검증 전 항목 (b5·b6, u1·u3, v2, σ대변동, k_event, 커버리지 정밀판정 등).
- 사전 등록 대조 최종표 (예상 vs 실측 vs 원인 판정).
- WEEK3_REPORT.md: 합격 관문(오차 중앙값 ≤0.45%·베이스라인 초과·커버리지) 판정 결과 + **판정 비대칭 문구 명기** ("양성은 입장권 — 60일 log-only로 진행").

## 7. 완료 기준 (DoD)

- [ ] β_mkt 채택안 + 근거 (OOS 오차 표) / 어긋남 ① 원인 판정 (a)/(b)
- [ ] σ_base 3레짐 확정치 (+provisional 플래그) + 커버리지 수치
- [ ] 총검증 최종 후보 성능표 + B1·B2 대조 + 다중검정 로그
- [ ] 가중 역산 결과 (2단 분할 증빙) + 미검증 가중 명시
- [ ] param_pack_v1.json 생성·스키마 검증 통과
- [ ] 사전 등록 최종 대조표 + O8 이관 목록

## 8. 하지 말 것

- 사전 등록과 어긋난 수치를 예상 범위로 "교정"하는 어떤 조작 (원인 판정과 기록만)
- 대변동 레짐 기준 변경, 관문 수치 완화
- 60일 로그 설계·G1A 관련 작업 (별도 발주)
- 스펙 문서 수정 (개정은 param_pack 확정 후 발주자가 일괄 수행)
