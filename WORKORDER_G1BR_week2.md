# WORK ORDER — G1B-R 구현 2주차: 직교화·채널 회귀·σ_base·AdjEx

- 발주 문서 버전: v1.0
- 대상: Claude Code (작업 디렉토리 `g1br/`, 1주차 산출물 위에서 진행)
- 상위 스펙: SPEC_G1B_R_beta_estimation.md v0.2 (§3 파이프라인), SPEC_G1B_gap_translation.md v0.3 (§3 채널 정의)
- 1주차 작업 규칙 5개조 전부 유지 (스펙 수정 금지 / 결정적 실행 / 룩어헤드는 사고 / 조달 시도 전량 기록 / 태스크당 커밋)

## 0. 1주차 실측의 발주 반영 (스펙 §2.1 대비 확정 변경 — 스펙 개정은 발주자가 추후 수행, 본 절이 우선한다)

| # | 변경 | 지시 |
|---|---|---|
| D1 | 금리 소스: FRED → 야후 (^TNX, 2YY=F) | **조건부 승인** — T1에서 검증 후 확정 |
| D2 | b5·b6(세션 질) 오프라인 제외 | 확정. 지수 사다리는 I0→I1→I2까지 (I3 없음). O8 이관 목록에 기재 |
| D3 | b4(Δfx) 정의 | T3에서 A/B 실측 판정 (판정 규칙 §3 참조) |
| D4 | B등급(야간선물 u1·TAIFEX u3) 조달 불가 | 60일 이관 확정. **오프라인 관측부 = GDR + GX만** — 가중 역산은 이 범위로 한정되고, param_pack의 u1·u3은 스펙 초기값 유지로 표기 |
| D5 | 락일 자동 수집 (KRX API) | 2주차 편입 — T5 (AdjEx) |

## 1. T1 — 금리 소스 검증 (D1 확정 절차)

1. 단위 정규화: ^TNX(수익률×10 지수)와 2YY=F(수익률 선물)를 **bp 단위 일변화**로 변환하는 함수 + 단위 테스트.
2. 절단 정합: "당일 Δy"의 정의를 명시 — 07:15 KST 스냅샷이 글로벡스 세션의 어느 가격인지(직전 체결 기준) night_panel 컬럼 주석에 기록. 2YY=F의 세션 캘린더 확인.
3. 교차검증: 2023~현재 일변화를 FRED(1일 지연 게시분)와 대조 — 상관 ≥ 0.95, 계통 편차 없음 확인. 미달 시 중단·보고.
4. 통과 시 config의 금리 소스를 야후로 확정 커밋.

## 2. T2 — ortho.py: 롤링 직교화 (스펙 §3.1)

- 순서 고정: fx→Δy2, SOXX→SPX, TSM→SOXX, peer→SOXX. **전기간 일괄 직교화 금지** — 각 직교화 회귀도 롤링 120일, 시점 t의 잔차는 t−1까지의 계수로 산출 (스모크 때의 전기간 방식을 대체).
- tests/test_ortho.py: ① 시점별 계수가 미래 데이터에 불변인지(룩어헤드 주입 테스트), ② 롤링 초기 120일 구간 NaN 처리, ③ 직교화 잔차와 설명변수의 표본내 상관 ≈ 0 확인.

## 3. T3 — b4(Δfx) A/B 판정 (D3)

- A안: 전일 KRW=X 종가 기준 Δfx (전 기간). B안: 60분봉 07:15 절단 Δfx (2024-08 이후만).
- 판정: **중첩 표본(2024-08~)에서만** I2 사다리의 OOS 오차 비교. B안 우위가 명확(오차 개선이 부트스트랩 80% 신뢰 이상)할 때만 B안, 아니면 **A안 채택** (긴 히스토리·단순성 우선).
- 시간 상한: 본 태스크 반나절. Δfx는 직교화 후 한계 기여가 작은 변수다 — 과투자 금지.
- 결과와 근거를 WEEK2_REPORT의 "D3 판정" 절에 기록.

## 4. T4 — channels.py: 채널 회귀 사다리 (스펙 §3.2, I3 제외)

```
지수: I0 = b1·r_SPX → I1 = +금리(b2, b3) → I2 = +fx_orth
고유: S0 = c1·soxx_ex → S1 = +peer_orth → S2 = +tsm_ex
결합: FairGap_hat = β_mkt·G_idx_hat + G_idio_hat
```
- 채널별 자기 라벨 (지수: KOSPI200 갭 / 고유: 종목갭 − β_mkt·지수갭). β_mkt 롤링 120일.
- OLS + Newey-West, 윈도 민감도 {60/120/250}, 주 1회 재추정 시뮬레이션.
- 사다리 채택 규칙: 상위 단계가 하위 단계 OOS 오차를 개선할 때만 채택. 각 단계의 인샘플 R²·OOS 오차를 표로.
- **해석·튜닝 금지는 유지** — 수치 보고까지가 2주차, 종합 판정은 3주차 워크포워드에서.

## 5. T5 — AdjEx (D5, 스펙 G1B v0.3 §3.4)

- KRX API로 배당·분할 이벤트 수집 → night_panel의 ex_date 플래그 완성 + AdjEx 계산 컬럼.
- 라벨과 FairGap 양쪽에 동일 적용됨을 테스트로 보증 (락일 표본에서 미조정 시 오차가 배당수익률만큼 발생하는 케이스 재현).

## 6. T6 — sigma.py: σ_base 레짐별 추정 (스펙 §3.3)

1. 레짐 캘린더 재구성: CPI·PPI·FOMC·고용 발표일(공개 캘린더 소스 조사, 조사 상한 2시간) + MU·NVDA 실적일. 소스 확보 실패 시 실적일만으로 부분 구성하고 보고.
2. I2·S2 결합 예측의 워크포워드 잔차(임시 — 3주차 총검증의 전신)를 레짐별 분리 → σ_base(평상/이벤트/대변동).
3. 커버리지 1차 점검: 1σ 커버율 산출 (판정은 3주차, 여기선 수치만).
4. 이벤트 밤 표본수 < 40이면 신뢰구간 병기 (스펙 §3.3-4).

## 7. 완료 기준 (DoD)

- [ ] D1 검증 통과·소스 확정 / D3 판정 완료 (근거 포함)
- [ ] test_ortho 포함 pytest 전체 green, 룩어헤드 위반 0건 유지
- [ ] 사다리 단계별 R²·OOS 오차 표 (삼전·하닉 각각) — **스펙 §5 사전 등록표 옆에 실측치 기입**
- [ ] σ_base 3레짐 수치 + 레짐별 표본수 + 1σ 커버율
- [ ] AdjEx 가동, 락일 케이스 테스트 통과
- [ ] WEEK2_REPORT.md: 위 전부 + 스펙 충돌 목록 + 3주차(워크포워드 총검증·가중 역산·param_pack) 착수 조건

## 8. 하지 말 것

- walkforward.py 총검증·weights.py 역산·param_pack 생성 (3주차)
- 사다리 결과를 근거로 한 변수 채택의 **최종** 확정 (수치 보고까지만 — 확정은 3주차 OOS 총검증)
- B·C등급 데이터 재조달 시도, 스펙 문서 수정
