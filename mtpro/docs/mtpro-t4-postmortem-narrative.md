# Gate R1 FAIL — 서술 전용 원인 기록 (1회, 2026-08-17)

- 지위: **기록용 서술.** 발주자 결정(8/17 밤) "원인 분석은 기록용으로만 1회". 새 측정·threshold 탐색·counterfactual 채점·부호 반전 시험 **없음** — 아래 수치는 전부 `mtpro-t4-gate-r1-result.md`·`logs/gate_r1_summary.json`·`mtpro-t3-report.md`에 이미 있는 값의 재서술이다.
- 판정은 변하지 않는다: **Gate R1 = FAIL 확정, Energy-Lite 종결(미채택·log-only), 2023-01~2026-06 표본 소진.** 기록판 `ENERGY_LITE_SPEC.md`.

## 1. 관찰된 사실 (기존 산출물)
1. **P1 flow IC_1은 3스코프 모두 음**, 000660은 −0.104로 CI가 0을 제외(사전 등록 부호 양과 유의하게 반대). 연도별로 000660은 4개 구간 전부 음(−.099/−.177/−.075/−.072); KOSPI200·005930은 2023~24 ≈0에서 2025~26H1 음으로.
2. **P2 breadth·P3-b Energy-Lite IC_21은 2023~24 강한 음(−.18~−.30) → 2025~26H1 양(+.10~+.40)** 으로 부호 반전. 전체 구간 IC는 상쇄되어 0 근처(−.04·−.02~−.04).
3. gradec_err(등급C)는 open→close 기준 β_SOX≈0으로 상수 모델화(D-A 명기) — Energy-Lite 3컴포넌트 중 하나는 "이벤트일 표준화 수익률"에 가까웠다.
4. Energy-Lite 컴포넌트 간 상관 ≈0(직교), baseline과 상관 <0.9(P3-a PASS) — 즉 "다른 신호였으나 수익률 정보는 없었다".
5. 서술 전용 baseline: MA20/60 스프레드의 IC_21 KOSPI200 +.17·005930 +.27(CI 0 포함).
6. 룩어헤드 절단 재산출 12회 위반 0, None 유지 규칙 충족(grep 1건은 보고용 함수) — 실패의 원인이 데이터 누수나 결측 처리가 아님을 뜻한다.

## 2. 사실에서 곧바로 따라오는 서술 (해석은 여기까지)
- **잣대 문제**: Gate R1의 주 잣대는 익일·익월 **수익률 IC**였다. MT-PRO의 주장은 "반응함수의 상태"이지 "수익률 예측"이 아니다. AM-9(반응함수 재현성)는 이 관문에서 측정되지 않았다 — Lite의 실패가 본 가설의 반증이 아닌 이유.
- **재료 문제**: 컨센서스(등급A) 0건·분봉 미사용·갭 제외(A-1) 상태의 소급 재료로는 Reaction 계열이 사실상 비어 있었다(사실 3). 완전판(T5)은 이 재료를 전부 갖는다 — Lite와 같은 것을 다시 재는 것이 아니다.
- **부호 반전 관찰(사실 2)**: 상승장 2025~26H1에 breadth·Energy가 양으로 돌아선 것은 "레짐 의존 부호"라는 **가설**을 낳지만, 이 표본에서 그것을 시험하거나 부호를 뒤집어 재채점하는 것은 금지(소진 표본). → challenger 가설 `flow_sign_regime_dependence`로 등록, 라이브 신규 표본 + 사전 등록으로만 검증(발주자 등재 ①).
- **000660 flow 음의 IC(사실 1)**: 기록만. 역부호 신호화 금지(사후 부호 채굴 차단).

## 3. 하지 않은 것 (의도적으로)
- 컴포넌트별 재조합·가중 재탐색·지평(1/5/21) 재선택·구간 재분할·부호 반전 채점 — 전부 소진 표본 재사용이라 실행하지 않았다.
- AM-9 기준으로 소진 표본을 재측정하는 것 — 잣대를 바꿔 같은 데이터에 두 번째 기회를 주는 일(기록판 §4 "재검증에 관한 규율").

## 4. 남긴 것
인프라(bronze/silver/gold·PIT·절단 재산출 테스트)·C-1 확정·β_SOX≈0 실측(A-1R의 보조 진단)·관문 규율의 실증. 다음 판정 재료는 T5/T5.5·Gate R2의 **새 규칙·새 표본**뿐이다.
