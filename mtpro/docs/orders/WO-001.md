# WO-001 — T5 계획서 조건부 승인 마감 5건 + 추가 등재 2건 (구현자 접수본, 2026-08-17)

- 지위: 발주자 지시 블록을 **받은 그대로** 발주서로 파일링(구현자 접수본). 발주자 정식 발송본이 오면 그것으로 대체. 근거 산문 = `../plans/PLAN-001.md`. 충돌 시 이 발주서가 이긴다.
- 반영 상태: 5건은 `docs/mtpro-t5-plan.md §12`에 명문화·커밋(cf76e2f·ddbbb17), 추가 등재 2건은 워크오더 "Gate R1 후속 결정" 절·`config/mtpro.yaml challenger_hypotheses`·T5 계획서 §9/§10에 등재.

---

## A. 마감 5건 (원문)

[T5 계획서 조건부 승인 — 아래 5건 명문화 후 T5-1 착수. 새 개념 추가 아님, champion 의미 고정]

① n_inference 통계 조건:
   Good/Bad 재현성 = H1 방향 사전등록·one-sided α=.05·power=.80·target ρ=.40 → n_inference=40 으로 명기.
   다중검정: 주 endpoint 2개(Good Acceptance 재현성·Bad Resilience 재현성)만 Gate 판정, 나머지 inference 항목은 secondary(기록·서술)로 사전 고정.

② family cap 알고리즘 확정:
   share_i = |w_i·s_i| / Σ|w·s| (절대기여 기준). share > 0.6이면 cap 후 초과분을 나머지 available family에 가중비례 재배분(1회). 재배분 후에도 초과 시 잔여분 버림. family score 범위·available 재정규화 식을 수식으로 명기.

③ Semi Transmission champion = 방법 A:
   SOXX = sector baseline β, NVDA/MU/TSM = SOXX 대비 residual shock의 전달 β. 이후 β_up − β_down 산출. raw 4-factor 동시 OLS는 champion·challenger 모두 불채택(불안정 계수).

④ 기준시점 고정:
   PSA σ20 = 충격일 t 제외, t-20~t-1 실현변동성 (lagged).
   Divergence z = 20일 slope를 직전 120거래일(t-1까지) slope 분포로 표준화. 120 = champion, 60/252는 challenger 등록만.

⑤ t0 동적 매핑:
   t0 하드코딩 금지 — event timestamp → Asia/Seoul 변환 → 이후 최초 XKRX session open으로 코드 계산 (KRX 휴장·대체공휴일 대응).
   삼성 3Q 잠정 10/8은 schedule_status=tentative로 표기, 공식 공지 시 confirmed 전환 (D-7 재확인 체크 연동).

승인 유지 항목 (변경 없이 진행): W_digest=5 champion(3/7일은 challenger만), 검증 쌍 e*당 1쌍 제한, Gap Reaction ERR_z 주 종속값·intraday ERR challenger, PSA t+5 pending, 상위 상태 confidence의 evidence/agreement 분리, Energy-Lite historical record 보존, T5.5 미달 항목 서술 전용 사전 선언.

5건 반영본 확인 즉시 T5-1 구현 착수. 이후 개념·설계 논의 추가 없음 — 다음 판정 재료는 전진 데이터.

## B. 추가 등재 2건 (원문)

① challenger 가설 등록: "flow_sign_regime_dependence" — Flow Impact 부호의 레짐 의존성. 근거 관찰: Gate R1 서술 기록의 연도별 부호 반전(2023~24 음 → 2025~26H1 양). 검증은 라이브 신규 표본 + 사전 등록으로만. 소진 표본 소급 인용 금지.
② T5.5 사전 등록 문서에 AM-9 판정 수치 예시 수록: Bad Resilience_t=+70 상태에서 이후 최초 독립 악재 — Expected −3.5% vs Actual −1.2% = 재현성 지지 / Expected −2.0% vs Actual −5.0% = 재현성 기각. (예시는 해석 기준 명확화용, 판정 조건 자체는 사전 등록 산식을 따름)

Energy-Lite 기록판(ENERGY_LITE_SPEC.md)을 mtpro/docs/에 등재할 것. → 등재 완료 `docs/ENERGY_LITE_SPEC.md`.

## C. 함께 확정된 결정 (같은 발주 단위)
- Gate R1 = FAIL 확정, Energy-Lite 종결(미채택·log-only), 2023-01~2026-06 표본 소진(재튜닝·부호 반전·지평 재탐색·amendment 재측정 금지), 000660 음의 IC 기록만, 원인 분석 서술 전용 1회(`docs/mtpro-t4-postmortem-narrative.md`), T5는 새 champion으로 진행, 심판은 T5/T5.5·Gate R2 새 규칙·새 표본.
