# 챌린저 사전 등록 — T2+ v2: 야간선물 기준점 + 잔여 drift 예측 (발주 D 2026-08-20)

- 등록일: 2026-08-20 (첫 판정 전 등록 — 등록 상수는 lib/g1a/t2plusV2.ts V2 블록에 고정, 변경은 재등록)
- 대체: 기존 T2+ 섀도(v1, w_nf 미니 Hedge) → **v2**. 구판 기록(t2.shadow)은 보존·분리 집계. 본판정 T2 무접촉.

## 구성 (등록 시점 고정)
- **기준점(수용)**: base = 야간선물 19:35 절단값 × β_mkt (예측하지 않고 받아들임).
  confidence_vol = 저녁 세션 거래량 / 동시간대 20일 평균 — **8/20부터 축적, 20일 전까지 null(강등 미적용 명기)**.
- **예측부(drift — 19:35 이후 야간선물의 잔여 방향만)**: 성분 ⓐ바스켓 마지막 30분 가속(문턱 0.15%) ⓑDC-NF(≥60%×누적 부호)
  ⓒDC-PM(≥60%×바스켓 부호) ⓓP1 유럽 종반 기울기(±0.3%, 종반 30분 미보유 시 세션 변화율 근사 — 명기)
  ⓔ이벤트 캘린더(밤중 발표 = 방향 예측 무효화) ⓕ매크로 저녁 변화율(10Y bp·달러원%·WTI% 17:00~19:35, 합의 ±2표).
  **원칙: 변화율·기울기·일관성만 — 레벨 절대 금지** (이중계상 + 구판 금리 항 OOS 유해 전례).
  fx 챌린저(I2)와 역할 구분: fx = R1 크기 / ⓕ = 저녁 drift 방향.
  출력: {상방/중립/하방} × 확신도(순동의 성분/4, 방향 선언 최소 2표).
- **최종**: 예상갭 = base + drift 조정(확신도 × 0.6% × β, 상한 ±0.5σ). 등급 4등급제 문법. 거래량 배율 <0.7 시 1단계 강등.
- **성분별 기여 로그** 분리 저장(components) — 절제 진단 가능 구조 (§5).

## 채점 이중 (§4)
ⓐ 최종 예상갭 vs 실측 (labels.v2.te_pct — 본판정·챔피언과 3자 비교) /
ⓑ **drift 방향 vs 실제 야간 궤적(19:35→06:00 부호, |0.15%| 미만 무판정)** — base 무임승차 차단 (labels.v2.drift_hit).

## 시간별 연속 트랙 (§5)
채점 정본 = 19:35 판정 불변. 20:30~06:00 매시 재판정은 관측·학습 전용(shadow_v2.hourly) —
성분은 축소판(ⓐ·ⓕ·누적 부호, reduced 표기) 고정: 성분 확장은 재등록 사항.
[이행 갱신 8/20 밤] 매시 확장 구현 — 20:30~06:00 시간당 1회(슬롯=시각 앞 2자리, /api/g1b/run 호출 시).
커버리지는 크론 격자에 종속: 격자 등록 전에는 기존 창(23시·03시·04:50~06:00) 시각만 기록(명기).

## 등록 가설
**"야간선물 기준점 수용 + drift 방향 예측이 v1(가산식)보다 예상갭 오차를 줄이고, drift 단독 채점(ⓑ)이 우연(50%)을 이긴다."**
승격: 20~30 거래밤 후 본판정 T2 대비 심사 — 승격 시 본판정 구조 교체는 헌법 개정 절차 (§6).
