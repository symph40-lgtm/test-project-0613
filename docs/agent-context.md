# 에이전트 공용 맥락 (두 PC 동기화용) — 2026-08-20 내보내기

발주자 승인(2026-08-20 밤): 에이전트의 로컬 메모리 핵심을 저장소 문서로 내보내 어느 컴퓨터에서 작업해도
같은 맥락을 보게 한다. **이 파일이 최신 정본** — 갱신은 의미 있는 변화가 있을 때 세션이 직접 한다.
(로컬 메모리는 컴퓨터마다 따로 쌓이므로, 상세는 각 항목이 가리키는 저장소 문서를 정본으로 삼는다.)

## 운영 원칙 (발주자 지시)
- **응답은 전부 한국어 존댓말** (2026-07-25 지시).
- **보고는 절대 날짜·시각** — "어제/오늘" 금지 (자정 넘는 세션의 혼동 방지).
- **발주 이행 대조표 규약** (2026-08-20): 발주문 접수 즉시 항목 분해(`g1br/orders/`), 보고 끝에 표 첨부, 빈 칸 = 검수 포인트.
- **세션 시작 루틴**: `npx tsx scripts/ops-pending.ts`로 ops_directives pending 확인 (모바일 /ops 지시).
- **G1 작업 규칙**: CLAUDE.md — WORKORDER가 유일한 범위 기준, specs/ 읽기 전용, 룩어헤드 발견 시 중단, 범위 확대는 발주자 경유.
- **문자 정책**: 단문(≤90바이트)=SMS(제목 없음), 장문=LMS. sms_pause 2026-08-21까지 — G1 알림은 이메일 대체.
  알림 문구는 추세추종 원칙 (급등을 "과열·청산"으로 쓰지 않기). 예측 문자는 신모델 전용 (NM_ONLY 허용키).

## 배포·환경
- Vercel 프로젝트 stockguard-0613 → 도메인 test-project-0613.vercel.app. 배포: `npx vercel --prod` (또는 GitHub 연동 확인).
- 환경변수 원본은 로컬 `.env.local`뿐 — **git에 없음**. 새 PC에는 수동 복사 필요. Supabase 마이그레이션은 수동 적용.
- 크론: cron-job.org(발주자 관리) + Vercel 크론(Hobby, 34~54분 지연 있음). 2026-08-20 밤부터 `/api/g1b/run` */10 종일 잡.
- KIS 토큰은 ops_settings.kis_token 공유 캐시 (중복 발급 금지).

## 진행 중 시스템 (정본 문서 위치)
- **G1A·G1B (일봉 갭 예측, 60일 log-only)**: 발주·대조표 `g1br/orders/`, 헌법 `g1br/constitution/`, 챌린저 등재 `g1br/challengers/`.
  현재 챌린저: v1.1c(야간선물 5번째 전문가)·T2+ v2(야간선물 기준점+drift, 첫 실기록 8/21 19:45)·적응 η(8/20 밤 등재).
  기준점 표기: 애프터比 우선 (종가比 괄호) — 채점의 자는 종가, 이원 기록.
- **MT 시장 톤**: 스펙 `docs/`·코드 `lib/mt/` — 톤 트랙만 유지(전환 트랙 3년 동결 8/16), /mt 페이지.
- **신모델 3종 (SOXX·하닉·삼전)**: 하닉 1박 유지·삼전 1박 중단 (8/8). 국장 1박 자격=동의일, 비중 100/50.
- **MT-PRO**: 별도 저장소 D:\vivecoding\mtpro (발주서 v10.1, T1 승인 게이트 대기) — 이 저장소와 무관.
- 그 외 이력(피셔·TOP10·PG-1 종결·NM3 등)은 로컬 메모리와 git 로그 참조.

## 두 PC 작업 규칙 (발주자 승인 2026-08-20)
1. 시작 전 `git pull`, 끝나면 커밋 후 `git push` — 한 번에 한 컴퓨터에서만 작업.
2. `.env.local`은 저장소 밖 — 새 PC 세팅 시 안전한 방법(USB 등)으로 복사, 절대 커밋 금지.
3. 이 파일과 `g1br/` 발주 문서가 세션 간 공용 맥락의 정본.
