# 레버리지·인버스 신호 시스템 기획 설계서 (v2.4)

> **v2.4 변경**: R6·R7·R8 복구 (일일 손실 한도·분할 진입·해외 상품 특칙 유지). v2.3에서 조정한 R2 트레일링 기본값 -4%(설정 변경 가능)와 D2(나스닥100) 판정 제외·기록 전용 전환은 그대로 유지.

> **v2.2 변경 핵심**: 진입 판정을 "갭 조건" 기반에서 **2축 정렬 모델(Bias × Confirmation)**로 전환. 축 1 = 매크로·펀더멘털이 가리키는 방향과 강도(비중 연동), 축 2 = 시가 이후 실제 형성되는 방향 지속성. 갭은 필수 조건에서 제외하고 유형 분류·진입 방식 조절용으로 강등. 반전형(7/3형)·연속형(gap-and-go)·무갭형 세 경로 모두 수용.
>
> **v2.0**: "추세일 판별 엔진" 중심 재설계. 매일 장 초반에 ①추세일/횡보일 판별 → ②방향 판정 → ③방향에 맞는 상품(레버리지/인버스) 진입의 3단계 구조. FKS200을 1차 신호원, 타국 지수 정합성을 2차 필터로 사용.

작성일: 2026-07-04 | 대상: 삼성전자·SK하이닉스 레버리지/인버스 ETP 단기 트레이딩 자동화

---

## 1. 목적과 범위

### 1.1 목적
사용자의 실전 검증된 트레이딩 규칙(2026년 6월 NFP 역발상 매수 성공사례 및 축적된 매매 원칙)을 정형화된 신호 체계로 변환하고, 조건 충족 시 알림·주문 지침을 제공하는 시스템을 구축한다.

### 1.2 범위
- 대상 상품: 국내 상장 삼전/하닉 관련 레버리지·인버스 ETF/ETN, 홍콩 CSOP 2배, 영국 3배 ETP(HNX3 등)
- 기능 범위: 데이터 수집 → 신호 판정(스코어링) → 알림 → (선택) 반자동 주문 보조 → 트레일링 스탑 관리 → 매매일지 자동 기록
- 1단계는 **알림 시스템**으로 시작하고, 자동 주문은 후순위(6장 로드맵 참조)

### 1.3 설계 원칙
1. **사람이 최종 결정, 시스템은 판단 보조**: 성공사례의 핵심이 "장중 분위기 해석"이었으므로, 완전 자동화보다 체크리스트 점수화 + 알림이 목적에 부합
2. **손실 제한 우선**: 모든 진입은 시세포착주문(-3%) 동반을 전제로 설계
3. **예외 규칙의 명시화**: "폭락 직후 갭상승엔 인버스 금지" 같은 예외를 하드코딩

---

## 2. 규칙 정형화 (Rule Engine 명세)

사용자 문서의 조건을 기계 판독 가능한 신호로 변환. 각 신호는 [필수/가점] 구분과 데이터 소스를 가진다.

### 2.1 공통 사전 조건 (매일 장전 판정)

| 코드 | 조건 | 판정 기준 | 소스 |
|---|---|---|---|
| C1 | 이벤트 캘린더 | 당일/익일 NFP·CPI·FOMC·실적발표·선물옵션만기 여부 태깅 | 경제캘린더 API |
| C2 | 리밸런싱 월 | 분기 첫째 달(1·4·7·10월)=순풍, 2·5·8·11월(MSCI)·분기말=역풍 | 달력 |
| C3 | 환율 | USD/KRW 전일 대비 방향 및 절대 수준(1500원 위/아래) | 시세 API |
| C4 | 미 금리 | 2년물·10년물 전일 변동, 상승 지속/안정/하락 3분류 | 시세 API |
| C5 | 밤사이 미국장 | 나스닥·필라델피아반도체(SOX) 등락률 | 시세 API |

### 2.2 진입 원리 (v2.2 — 2축 정렬 모델)

**갭은 필수 조건이 아니다.** 레버리지 진입 경로는 세 가지 모두 유효하다: ①갭하락 출발 → 반전 후 상승 지속(7/3형), ②갭상승 출발 → 계속 상승(gap-and-go), ③무갭 출발 → 계속 상승. 인버스도 거울상으로 세 경로(갭상승→반전 하락, 갭하락→연속 하락, 무갭→연속 하락). 갭의 역할은 진입 가부 결정이 아니라 **유형 분류(T7)와 진입 방식 조절**(큰 갭일수록 시초 진입 금지, 확인 후 진입)로 한정한다.

진입은 두 축의 정렬로 판정한다:

**축 1 — Bias (매크로·펀더멘털 방향 점수)**: 오늘 가격이 "가야 마땅한" 방향과 그 강도. 구성: 금리·환율 방향(C3·C4), 전일 이벤트·지표의 서프라이즈 방향(L10), 이익 컨센서스 유지 여부(L8), 낙폭/상승폭의 원인 성격(L7). 산출: 상방 Bias / 하방 Bias / 중립, 각각 강도 1~3등급. **매크로 개선의 강도가 클수록 허용 비중이 커진다** — 이벤트가 좋아지고 펀더멘털이 유지되면, 그 강함의 정도에 비례해 레버리지 사용.

**축 2 — Confirmation (가격 행동 확인)**: 시가 이후 실제로 형성되는 방향 지속성. 구성: FKS200의 방향 형성과 유지(반전이든 연속이든), 실시간 DC1/DC2 (2.5.6), 되돌림 깊이(T3), 방향 전환 횟수(T6). 판정: "한 방향이 형성되어 어느 정도 유지되는 구간이 확인되는가."

**진입 규칙**: Bias 방향 = Confirmation 방향으로 정렬될 때만 진입. 정렬의 대표 시나리오:
| 시나리오 | Bias | 가격 초반 | 판정 |
|---|---|---|---|
| 괴리 후 반전 (7/3형, 성공사례 원형) | 상방(매크로 개선 + 펀더멘털 무손상) | 하락 지속 = **괴리(이상 신호), 감시 강화** → 반전 발생 → 반전 후 진행 강도 확인 | 강한 진행 확인 시 레버리지. 괴리 자체는 진입 신호가 아니라 "반전을 기다리는 상태" |
| 순방향 연속 (gap-and-go) | 상방 | 처음부터 상승 지속 (갭 유무 무관) | Confirmation 충족 즉시 레버리지, 비중은 Bias 강도 비례 |
| 하방 정렬 | 하방(매크로 악화·악재) | 하락 형성·지속 (갭 유무 무관) | 인버스 |
| 불일치 | 상방 | 하락 지속·반전 기미 없음 | **진입 금지, 관찰만** — Bias가 맞다고 가격에 역행해 미리 사지 않는다. 가격이 돌아설 때까지 대기 |

### 2.2.1 레버리지(롱) 진입 신호

**필수 조건 (모두 충족해야 후보)**
| 코드 | 조건 | 판정 기준 |
|---|---|---|
| L1 | Bias 상방 | 축 1 판정이 상방 (금리·환율 안정 이상 + 이벤트·지표 우호 방향), 강도 1~3 산출 |
| L2 | 금리·환율 안정 | C3 하락 or 보합 AND C4 상승 아님 (Bias의 하위 구성이자 단독 필수) |
| L3 | 방향 형성 확인 | FKS200이 상방으로 방향을 형성·유지: (a)하락 → 축소 → 상승 반전 후 **반전 방향 진행이 강하게 유지**(되돌림 얕음, 실시간 DC1 상방 우위), 또는 (b)초반부터 상승 지속. 반전형은 반전 직후가 아니라 **반전 후 지속 확인 뒤** 진입 |
| L4 | 진입 시간대 | 09:30 ~ 10:30 사이에만 진입 (09:00~09:30은 관찰 전용) |
| L5 | 외인 수급 (3요소) | **순매수 전환을 요구하지 않음** (구조적 매도 국면 대응). ①수준: 10:00까지 누적 순매도 ≤ 기준치(초기값 1.1조) ②기울기: 30분 구간별 순매도 증가분이 감속 중(뒤 구간 ≤ 앞 구간) ③상대강도: 당일 매도 페이스가 최근 20일 일평균 순매도의 1.2배 이하. 3개 중 ①+② 필수, ③은 가점 |

**가점 조건 (점수 가산, 4장 스코어링 참조)**
| 코드 | 조건 |
|---|---|
| L6 | 직전 1~3일 누적 -12% 이상 과대 낙폭 (V반등 후보) |
| L7 | 낙폭의 원인이 수급·지정학·소송 등 비(非)실적 요인 |
| L8 | 증권사 이익 컨센서스 유지·상향 중 |
| L9 | 개인·기관이 외인 물량 흡수 중 |
| L10 | 전일 매크로 서프라이즈가 완화적(NFP 부진 → 금리인상 우려 후퇴 등) |
| L11 | 기대인플레이션 하락 추세 |

**금지 조건 (하나라도 해당 시 진입 불가)**
- X1(수정): 갭상승 +2% 초과 출발일은 **시초 직후 추격 진입만 금지** — L3(b) 상승 지속이 09:30 이후 확인되면 진입 허용 (gap-and-go 유형 수용. 단 큰 갭일수록 확인 구간을 길게)
- X2: 당일 밤 바이너리 이벤트(NFP/FOMC/실적) 예정인데 오버나잇 계획인 경우 — 단, 예외규칙 E1 참조
- X3: 당일 손실 한도 이미 도달

### 2.3 인버스 진입 신호

**필수 조건**
| 코드 | 조건 | 판정 기준 |
|---|---|---|
| S1 | 과열 또는 하방 Bias | 직전 2일 이상 연속 상승·5일 누적 +15% 이상(과열 반전형), **또는** 축 1 판정이 하방(악재·매크로 악화 주도형 — 연속 하락일도 대상) |
| S2 | 매크로 악화 | 금리 상승 or 환율 상승 or 채권가 하락 중 최소 2개 |
| S3 | 하방 형성 확인 | FKS200이 하방으로 방향 형성·유지: (a)상승 → 축소 → 하락 반전 후 진행 지속(과열 페이드형), 또는 (b)초반부터 하락 지속(갭하락·무갭 연속 하락형 포함). **갭상승 출발은 필수 아님** — 갭은 유형 분류용 |
| S4 | FKS200 꺾임/지속 | 반전형은 꺾임 후 하방 진행 확인, 연속형은 하방 유지 확인 (실시간 DC1 하방 우위) |

**가점 조건**
- S5: 나스닥·일본 등 타국 하락 중인데 한국(하닉)만 상승 — 디커플링 과열
- S6: 악재 뉴스 발생(공급과잉 우려, 소송, 규제 등)
- S7: 매크로 발표가 컨센서스 수준 → 금리인상 가능성 유지

**금지 조건 (2026.6월 데이터 검증에서 발견된 핵심 예외)**
- **XS1: 직전 1~3일 폭락(누적 -12% 이상) 후의 갭상승은 V반등이므로 인버스 절대 금지** (6/9 갭+7.3%→장중 +8.1%, 6/25 갭+11%→장중 +1.8% 사례)
- XS2: 폭락 원인이 비실적 요인이고 이익 컨센서스가 상향 중인 구간에서 인버스 금지

### 2.4 청산·리스크 규칙

| 코드 | 규칙 |
|---|---|
| R1 | 진입 즉시 시세포착주문(손실제한) **-3%** 설정 |
| R2 | 이익 발생 시 손절선을 현재가 기준 **-4%(기본값, 설정 파일에서 변경 가능)**로 상향 수정(트레일링). 손절선은 이익 방향으로만 이동, 절대 하향 금지 |
| R3 | 당일 청산 원칙: 15:00 지정가 분할 청산 (동시호가 시장가 지양) |
| R4 | 예외 E1: ADR 상장일, 삼전/하닉 실적 발표 전일 등 밤사이 호재 예정일만 평소 1/3 이하 소량 오버나잇 허용 |
| R5 | 비중: 인버스는 총자산의 5%(고확신 10%) 상한. 레버리지 롱도 1회 최대 비중 사전 정의. **Bias 강도 연동**: 강도 1 = 최대 비중의 1/3, 강도 2 = 2/3, 강도 3(매크로 개선 강함 + 펀더멘털 견고 + Confirmation 강함) = 최대 비중 |
| R6 | 일일 손실 한도: 계좌 -1% 도달 시 당일 거래 전면 중단, 재진입 금지 |
| R7 | 분할 진입: 시초 확인 1/3 → 지지 확인 1/3 → 방향 확인 1/3 |
| R8 | 해외 상품 특칙: CSOP(홍콩)는 한국장과 병행 운용 가능. 영국 3배(HNX3)는 한국 장중 대응 불가 → 한국 종가와 익일 시가 시나리오 기반으로만 운용, 바이너리 이벤트 전 보유 금지 |

---

## 2.5 추세일 판별 엔진 (v2.0 신규 — 시스템의 중심 모듈)

### 2.5.1 문제 정의
매일의 장은 크게 세 가지다: ① 초반에 방향을 잡고 종일 유지하는 **추세일**(갭하락 후 계속 하락 → 인버스 / 갭상승 후 계속 상승 → 레버리지), ② 방향 없이 왔다갔다 하는 **횡보일**(레버리지 상품엔 최악 — 스탑 왕복 손실), ③ 초반 방향이 장중 뒤집히는 **반전일**(V반등·페이드, 기존 2.2~2.3의 셋업). 시스템의 1차 임무는 09:00~10:00 데이터로 오늘이 어느 유형인지 조기 판별하는 것이다.

사용자 관찰의 정형화: "FKS200은 한번 방향을 잡으면 그 방향이 유지되는 경향이 있고, 국내 현물은 변동이 심해 방향을 못 잡는다" → 코스피200 선물은 외국인·프로그램 매매가 주도해 방향성이 깨끗하고, 현물 개별주는 노이즈가 크다. 따라서 **방향 판정은 선물(FKS200)로 하고, 실제 매매는 하닉/삼전 레버리지·인버스로 실행**하는 이원 구조가 합리적이다.

### 2.5.2 추세일 판별 신호 (T-시그널)

| 코드 | 신호 | 판정 기준 | 논리 |
|---|---|---|---|
| T1 | 시초 레인지 이탈 | 09:00~09:30 선물 고가·저가(Opening Range)를 09:30 이후 한 방향으로 이탈 후 재진입 없음 | 추세일의 고전적 정의(ORB). 레인지를 위로 깨면 상승 추세 후보, 아래로 깨면 하락 추세 후보 |
| T2 | VWAP 편측성 | 선물 가격이 당일 VWAP의 한쪽에만 머무는 시간 비율 ≥ 80% (10시 기준) | 추세일은 VWAP 재터치 없이 이격을 벌림. 횡보일은 VWAP을 반복 관통 |
| T3 | 되돌림 깊이 | 직전 스윙 파동의 되돌림이 40% 미만으로 얕음 | 얕은 눌림 = 매수(매도)세가 기다리지 않고 쫓아옴 = 추세 지속 신호 |
| T4 | 외국인 선물 수급 | 외국인 코스피200 선물 순매수(도) 잠정치가 한 방향으로 누적 증가. 현물이 구조적 매도 국면일 때는 부호가 아닌 **기울기 변화(매도 감속=상방, 매도 가속=하방)**로 판정 | 추세일의 동력원. 외인 선물 방향과 가격 방향 일치 시 신뢰도 급상승 |
| T5 | 프로그램 매매 방향 | 차익+비차익 프로그램 순매수(도)가 가격과 동일 방향 | 선물 주도 장세의 현물 전이 확인 |
| T6 | 방향 전환 횟수 | 09:00~10:00 사이 5분봉 기준 고점/저점 갱신 방향의 전환 횟수 ≤ 2회 | 3회 이상 뒤집히면 횡보일로 분류, 당일 추세 매매 금지 |
| T7 | 갭과 초반 방향의 일치 | 갭 방향과 첫 30분 진행 방향이 같음 (gap-and-go) | 갭+동방향 진행 = 추세일 확률 상승. 갭과 역방향 진행 = 반전일 후보(2.2/2.3 셋업으로 라우팅) |
| T8 | 거래대금 확장 | 진행 방향 5분봉의 거래대금이 반대 방향 봉 대비 우위 | 방향에 돈이 실리는지 확인 |

### 2.5.3 정합성 검사 (Cross-Market Divergence — 2차 필터)

사용자 관찰의 정형화: "일본은 오르는데 한국만 내리면 이상하다" → 동조화 이탈은 두 가지 의미를 가진다.

| 코드 | 검사 | 해석 |
|---|---|---|
| D1 | 니케이225 선물 vs FKS200 | 같은 방향 = 추세 신뢰도 가점. 역방향 = 이탈 경보 |
| D2 | 나스닥100 선물(장중 시간외) vs FKS200 | **판정 미사용, 기록만** (v2.3): 실측상 나스닥 하락일에도 한국 반도체 상승 사례 확인 — 외인 리밸런싱·국내 고유 동력 국면에서 정합성 낮음. 학습 데이터로만 축적해 유효 레짐 여부를 8장에서 사후 검증 |
| D3 | 대만 자취안(반도체 동조) vs 하닉 | 반도체 섹터 고유 흐름 분리 |
| D4 | 이탈 원인 분류 | 한국 고유 악재·호재 존재 시(뉴스·수급) → 이탈은 정당, 추세 지속 가능. 고유 원인 부재 시 → 과매도·과열 왜곡, **반전(평균회귀) 셋업으로 라우팅** (성공사례: 일본 상승 속 한국만 하락 → 과대 눌림 판단 → 롱) |

핵심 규칙: **정합(타국과 동방향) 추세는 추세추종으로, 이탈(한국만 역방향) 상태는 원인이 없을 때만 역발상으로.** 이탈인데 한국 고유 원인이 있으면(예: 6/23 전쟁+수급) 그 방향의 추세일로 취급한다.

### 2.5.4 일일 의사결정 트리 (통합)

```
08:30 장전 브리핑 (C1~C5)
  └ 이벤트일? → 보수 모드(비중 절반) 또는 관망
09:00~09:30 관찰 (진입 금지, 데이터 수집만)
  └ 갭 크기·방향 기록, Opening Range 확정
09:30~10:30 판정 및 진입
  ├ [분기 1] 직전 1~3일 누적 -12% 이상 폭락 후?
  │    └ YES → 반전(V반등) 셋업 우선 (2.2 L6~L10). 인버스 금지(XS1)
  ├ [분기 2] T-스코어 ≥ 기준 (추세일 판정)?
  │    ├ 방향 UP + D1·D3 정합 → 레버리지 진입 (분할 R7)
  │    ├ 방향 DOWN + D1·D3 정합 → 인버스 진입 (비중 R5)
  │    └ 타국과 이탈 + 한국 고유 원인 없음 → 역발상 셋업 검토 (2.2/2.3)
  └ [분기 3] T6 위반 (방향 전환 3회 이상) → 횡보일 선언, 당일 추세 매매 금지
10:30 이후: 신규 진입 없음. 보유분은 R2 트레일링 관리
15:00: 당일 청산 (R3), 예외는 R4
```

### 2.5.6 방향 지속률(Directional Consistency) — 추세일의 정량 라벨 (v2.1)

추세일을 사람의 인상이 아니라 수치로 정의한다. 두 지표를 병행 산출:

| 지표 | 정의 | 해석 |
|---|---|---|
| DC1 (동방향 봉 비율) | 당일 **10분봉** 중, 그날의 순방향(종가-시가 부호)과 같은 방향으로 마감한 봉의 비율 | **60% 이상 = 추세일 후보** (실측 근거: 7/3 +30% 수익일의 10분봉 양봉 비율이 65% — 강한 추세일도 봉 단위로는 눌림봉이 3분의 1 섞임), 50% 부근 = 횡보일 |
| DC2 (효율비, Efficiency Ratio) | \|종가-시가\| ÷ Σ\|10분봉 변화폭\| (0~1) | 이동 경로 대비 순이동 거리. DC1이 60%대여도 양봉의 평균 크기가 음봉을 압도하면 DC2가 높게 나옴 — 봉 개수가 못 잡는 '크기의 비대칭'을 포착 |

**운용 원칙**
- 임계값 θ(초기값: **10분봉 DC1 60%** / ER 0.30)는 **고정 상수가 아니라 최적화 대상 파라미터** — 8장 학습 시스템에서 어떤 수치가 손익을 극대화하는지 탐색 (그리드: 55/60/65/70%, 봉 주기 5분/10분/15분도 함께 탐색)
  - *(2026-07-05 운용 변경: 봉 주기 5분 · DC1 55% · ER 0.25로 시작 — 판정 확정 최소 대기를 30분→15분으로 줄이기 위한 사용자 결정. 잘은 봉의 노이즈 증가에 맞춰 임계값 비례 보정. 실데이터 축적 후 Stage 3에서 재탐색)*
- DC1 단독 판정의 허점 보완: 60%는 횡보일과의 경계가 가까우므로, **추세일 확정은 DC1 ≥ θ AND DC2 ≥ 기준의 동시 충족**으로 판정 (봉 개수 + 크기 비대칭 이중 확인)
- 두 지표는 사후 라벨(그날이 추세일이었는가)로 쓰이며, 장중에는 "현재까지의 DC1/DC2"를 실시간 계산해 T-스코어 보조 및 **추세 붕괴 청산 신호**(보유 중 DC1이 임계 밑으로 하락 = 추세 소멸 → 조기 청산)로도 사용
- 상방 지속일 = 레버리지 적합일, 하방 지속일 = 인버스 적합일, 중간 = 매매 부적합일 — 3클래스 라벨

### 2.5.7 6월 데이터 재검증 (추세일 관점)

| 날짜 | 유형 | T-신호 재구성 | 결과 |
|---|---|---|---|
| 6/23 | 하락 추세일 | 갭 -0.7% 후 초반부터 일방향 하락(T1↓·T7 일치), 전쟁 악재로 이탈 정당(D4) | 장중 -11.8% — 인버스 추세추종의 교과서 |
| 7/3 | 반전→상승 추세일 | 갭 -2.8%, 초반 추가 하락(저가 -8%대) 후 반전, 이후 일방향 상승 | 반전 확인 후 진입해도 장중 +10% 이상 확보 |
| 6/17, 6/22 | 상승 추세일 | 갭 소폭 하락 → 초반 상승 전환 후 유지(gap-fade 후 추세) | 장중 +8.0% / +7.0% |
| 6/12 | ~~하락 추세일(페이드)~~ → **막판 급락일 (정정)** | ~~갭 +8.6% 과다 → 초반부터 하락 진행~~ | 장중 -5.7% |

> **정정 (2026-07-04, 일봉 실측)**: 6/12는 "초반부터 하락"이 아니었다. 실측 일봉 = 시가 2,281,000(갭 +8.6%) / 고가 2,304,000(시가 대비 +1.0%뿐) / 저가·종가 2,150,000 — **갭 후 시가 부근에서 유지·등락하다가 장 후반 급락, 저가 마감**. 진입 창(09:30~13:30) 내에 하방이 형성되지 않았으므로 이 시스템의 정답은 **미진입**이다 (막판 급락은 따라갈 수 없는 유형 — "안 하는 날"). 다음 거래일 6/15는 갭 +6.2%로 급락분을 대부분 회복. 이에 따라 4.4 표의 6/12 "인버스 진입" 판정 예시도 무효 — 백테스트는 이 날을 '미진입이 정답'인 특이도 케이스로 검증한다.

시사점: 28거래일 중 장중 ±5% 이상의 일방향 추세일이 약 8~9일 — **약 3일에 1번꼴로 기회가 존재**했다(단, 고변동 구간 기준). 나머지 날에 "안 하는 것"이 이 시스템의 절반이다.

---

## 3. 시스템 아키텍처

```
[데이터 수집 계층]
  ├ 국내 시세: 증권사 OpenAPI (한국투자증권 KIS Developers 권장 / 나무증권은 자동주문 API 미제공 시 시세만 대체)
  ├ 선물 지수: KOSPI200 선물 실시간 (KIS API 선물 시세)
  ├ 수급: KRX 정보데이터시스템 or 증권사 API 투자자별 매매동향 (외국인 실시간 잠정치)
  ├ 매크로: USD/KRW·미 국채 2y/10y·나스닥·SOX (무료: 야후파이낸스 / 유료: 인베스팅 비공식은 불안정)
  ├ 크로스마켓 실시간: 니케이225 선물·대만 자취안 (D1·D3 정합성 검사) + 나스닥100 선물 (D2 기록용)
  ├ 선물 수급: 외국인 코스피200 선물 순매수 잠정치·프로그램 매매 동향 (T4·T5용, 증권사 API)
  ├ 경제 캘린더: NFP/CPI/FOMC 일정 (수동 월 1회 입력 or API)
  └ 뉴스 트리거: 키워드 알림(선택, 2단계)

[신호 엔진]
  ├ 장전 브리핑 모듈 (08:30): C1~C5 판정 + 당일 모드 결정 (추세 후보일 / 반전 후보일 / 이벤트 보수일)
  ├ ★ 추세일 판별 모듈 (09:00~10:30, 1분 주기): T1~T8 계산, 추세/횡보/반전 3분류 + 방향 판정
  ├ 정합성 검사 모듈: D1~D4 크로스마켓 비교, 이탈 시 원인 분류(뉴스 존재 여부)
  ├ 셋업 판정 모듈: 추세일→추세추종(방향별 레버리지/인버스), 반전일→L/S 셋업(2.2~2.3)
  └ 예외 필터: X1~X3, XS1~XS2 하드 블록 (모든 판정에 우선)

[실행 계층]
  ├ 알림: 텔레그램 봇 (스코어, 충족/미충족 조건 목록, 권장 행동)
  ├ 주문 보조: 진입가 기준 -3% 시세포착주문 수치 자동 계산 제시
  └ (3단계) 반자동 주문: 사용자가 알림에서 승인 버튼 → API 주문

[관리 계층]
  ├ 포지션 모니터: 보유 중 가격 추적, 트레일링 스탑 수정 시점 알림 (R2)
  ├ 매매일지: 진입/청산/조건 스냅샷 자동 기록 (CSV/스프레드시트)
  └ 성과 분석: 패턴별(V반등/추세/페이드) 승률·손익비 월간 리포트
```

---

## 4. 스코어링 체계 (판정 로직)

### 4.1 레버리지(롱) 스코어
- 필수 L1~L5: 하나라도 미충족 → 판정 "대기"
- 가점: L6(+3), L7(+2), L8(+2), L9(+1), L10(+2), L11(+1) — 만점 11
- 판정: 필수 충족 + 가점 5점 이상 = "진입 후보(1/3 비중)", 8점 이상 = "강한 신호(분할 계획 전체 가동)"
- 금지 X1~X3 해당 시 점수 무관 차단

### 4.2 인버스 스코어
- 필수 S1~S4 충족 + 가점 S5(+2), S6(+2), S7(+1) — 만점 5
- 판정: 필수 충족 + 2점 이상 = 진입 후보. **XS1·XS2는 최우선 차단**
- 인버스는 기본 비중을 롱의 절반으로 (R5)

### 4.3 추세일(T) 스코어 — 최상위 판정 (v2.0)
- T1(+3), T2(+2), T3(+1), T4(+3), T5(+1), T7(+2), T8(+1) — 만점 13 / T6 위반 시 즉시 "횡보일" 선언
- 판정: **8점 이상 + D1·D3 정합** = 추세일 확정 → 방향대로 추세추종 진입 (UP=레버리지, DOWN=인버스)
- 5~7점 = 약한 추세 → 1/3 비중만, 트레일링 타이트(-2%)
- 4점 이하 or T6 위반 = 횡보일 → 당일 추세 매매 금지, 반전 셋업(2.2~2.3)만 감시
- 크로스마켓 이탈(D4) 시: 한국 고유 원인 있음 → 추세로 취급 / 원인 없음 → 역발상 셋업으로 라우팅
- 우선순위: 예외 필터(X·XS) > 폭락 후 V반등 분기 > T 스코어 > L/S 셋업 스코어

### 4.4 판정 예시 (2026.6월 실데이터 기반 백테스트 시나리오)
| 날짜 | 상황 | 시스템 판정 | 실제 결과 |
|---|---|---|---|
| 6/9 | 이틀 -17% 후 갭+7.3% | 롱: L6·L7 충족이나 X1(갭+2%초과)로 시초 진입 차단 → 장중 눌림 시 재판정 / 인버스: XS1 차단 | +15.9% (인버스 진입했다면 대참사) |
| 6/12 | 반등 랠리 후 갭+8.6% | 인버스: S1·S3 충족, S4 확인 시 진입 | 장중 -5.7% (인버스 2배 +11%) |
| 6/23 | 5연속 상승+28% 후 전쟁 악재 | 인버스: S1·S2·S6 충족, S4 트리거 | 장중 -11.8% (인버스 2배 +23%) |
| 7/3 | 전일 폭락, ~~갭-2.8%~~ **갭+0.5%(무갭, 정정)** | 롱: L1·L6·L7·L8 충족, L3 반전 대기 | 장중 +10.4% (고가 +11.7%) |

> **정정·보정 (2026-07-04, 일봉·분봉 실측 전수 대조)**
> - **7/3**: 갭은 -2.8%가 아니라 **+0.5%(사실상 무갭)**. 실측 경로 = 09:00 +1.6% → **09:59 저점 -6.4%** → V반전 → 종일 상승 +10.9% 마감. "괴리 후 반전" 원형은 유효하나 갭 수치 오기. 반전 후 지속 확인(시가 회복+상승)은 10:30~11:00에야 성립 — 진입 창 13:30 연장의 실측 근거.
> - **6/12**: 2.5.7 정정 참조 (초반 하락 아님 → 막판 급락, 시스템 정답 미진입). "반등 랠리 후"도 오기 — 직전 5일 누적은 약 -9%로 과열 아님.
> - **6/25 (XS1 필수 사례)**: 직전 1~3일 누적 최악 실측 **-11.6%** — 초기 임계값 -12%로는 XS1이 **미발동**해 갭+11% 날 인버스를 차단하지 못함. 이에 crashCumPct 초기값을 **-11%로 조정** (7.2의 튜닝 허용 범위 -8~-15% 내).
> - 6/9(-16.9%·갭+7.3%·+15.9%), 6/17(갭-2.0%·장중+8.0%·저가 0%), 6/22(+7.0%), 6/23(5연속+27.5%·갭-0.7%·-11.8%)은 실측과 일치 확인.

---

## 5. 데이터 소스 명세 및 제약

| 데이터 | 1순위 | 대안 | 갱신 주기 | 비고 |
|---|---|---|---|---|
| 하닉/삼전/ETP 시세 | KIS OpenAPI | 키움 OpenAPI+ | 실시간 | 나무증권(NH) 계좌 유지하며 시세·주문 API만 타사 계좌 병행 검토 |
| KOSPI200 선물 | KIS 선물 시세 | 인베스팅 웹(참고용) | 실시간 | FKS200 반전 판정의 핵심 입력 |
| 외국인 수급 잠정 | 증권사 API 투자자동향 | KRX 공표(장중 잠정) | 5~10분 | "10시까지 1.1조" 판정용 |
| USD/KRW | 서울외국환중개/야후 | 증권사 API | 실시간 | |
| 미 2y/10y·나스닥·SOX | 야후 파이낸스 | FRED(일간) | 전일 종가+장전 | 장전 브리핑용 |
| HNX3.L | LSE 직접/야후 | — | 지연 주의 | 검색엔진 시세는 스테일, 기존 확인사항 |
| 니케이225 선물 | 야후(NKD=F 등) | 인베스팅 웹 | 실시간~지연 | D1 정합성 검사. 한국장과 동시간대 거래 |
| 나스닥100 선물 | 야후(NQ=F) | — | 실시간~지연 | D2 기록용 (판정 미사용, 학습 데이터 축적) |
| 대만 자취안 | 야후(^TWII) | — | 실시간 | D3. 반도체 섹터 동조 확인 |
| 외국인 선물 순매수 잠정 | KIS API 선물 투자자동향 | 인포맥스 등 유료 | 5분 내외 | T4 — 추세일 동력 판정의 핵심. 잠정치 오차 유의 |
| 프로그램 매매 동향 | KIS API / KRX | — | 5~10분 | T5 |

**제약사항**
- 시세포착주문(스탑) 자체는 증권사 HTS/MTS 기능을 사용하고, 시스템은 "설정할 수치"를 계산·알림 (자동 설정은 3단계)
- 외국인 잠정 수급은 확정치와 오차 존재 — 임계값에 ±10% 버퍼 적용
- **임계값 정규화 원칙**: "1.1조" 같은 절대 수치는 매도 국면 강도에 따라 의미가 변하므로, 고정값이 아니라 "최근 20일 일평균 외인 순매도 대비 배율"로 저장·판정 (레짐이 바뀌어도 자동 적응). 검증 사례: 급등일 종일 매도 2조도 당시 일평균 대비 과대하지 않았음
- 자동주문은 증권사 API 약관·본인인증 요건 확인 필요. 완전 자동매매는 리스크가 크므로 본 설계는 반자동(승인 기반)까지만 권장

---

## 6. 개발 로드맵

**Phase 1 — 장전 브리핑 + 추세일 판별 알림 봇 (3~4주)**
- Python + 텔레그램 봇. 08:30 브리핑(당일 모드 판정), 09:00~10:30 1분 주기 T-스코어 산출
- 산출물: "추세일/횡보일/반전일 + 방향 + 스코어" 알림, 시세포착주문 수치 계산
- 성공 기준: 6월 급등/급락일 재현 테스트에서 4.4 표와 동일 판정. 특히 횡보일을 횡보일로 걸러내는 특이도(진입 안 함 판정의 정확도)를 승률보다 우선 검증 — 추세일 오탐 1회가 정탐 2회의 이익을 상쇄하기 때문
- 주의: T-스코어의 분봉 단위 검증에는 1분/5분봉 과거 데이터가 필요. KIS API 분봉 조회 또는 수동 축적으로 확보

**Phase 2 — 페이퍼 트레이딩 + 백테스트 (4주)**
- 알림대로 가상 매매 기록 1개월 → 패턴별 승률·손익비 측정
- 6개월 과거 데이터 백테스트 (일별 OHLC + 수급 데이터 확보 필요: KRX 정보데이터시스템에서 CSV 다운로드)
- 임계값 튜닝: 갭 기준(±2%), 과대낙폭 기준(-12%), 외인 매도 기준(1.1조)의 민감도 분석

**Phase 3 — 반자동 주문 + 트레일링 관리 (선택)**
- 알림 내 승인 버튼 → API 주문 실행, 포지션 보유 중 R2 트레일링 수정 알림 자동화
- 전제: Phase 2에서 실계좌 대비 신호 정확도 검증 완료

**Phase 4 — 지속 학습 루프 가동 (8장, Phase 1과 병행 시작 가능)**
- 장후 배치(라벨링·예측 대조·원인 주석 수집)는 Phase 1부터 즉시 가동 — 데이터 축적이 병목이므로 가장 먼저 시작할 것
- Stage 1 규칙 발견 리포트는 2개월 축적 후, Stage 2 확률 모델은 6개월+ 후 착수
- 버전 관리: 모델·파라미터·규칙 변경 이력을 매매일지와 연결해 "어떤 버전이 어떤 성과를 냈는지" 추적 가능하게 설계

---

## 7. 리스크 및 유의사항

1. **표본 한계**: 검증 데이터는 28거래일(초고변동 구간)로, 평온장에서는 신호 발생 빈도가 월 3~5회로 감소하고 V반등 패턴 승률이 하락할 수 있음. Phase 2 백테스트로 보정 필수
2. **과최적화 경계**: "1.1조", "-12%" 같은 임계값은 특정 구간에 맞춘 수치. 범위 테스트(0.8~1.4조, -8~-15%)로 강건성 확인
3. **6/23형 돌발 리스크**: 평범한 갭으로 출발해 장중 -12% 가는 날은 어떤 진입 규칙도 못 피함 → R6(일일 한도)와 R1(즉시 스탑)이 유일한 방어선
4. **레버리지 ETP 구조 비용**: 당일 청산 원칙(R3)이 변동성 끌림을 차단하는 핵심 장치이므로, 예외(R4)는 반드시 소량 원칙 유지
5. 본 문서는 판단 보조 시스템 설계이며 투자 결과에 대한 책임은 사용자에게 있음. 자동매매 관련 규정 및 증권사 약관 준수 필요

---

## 8. 지속 학습 시스템 설계 (v2.1 신규)

목표: "추세일(DC1 ≥ θ)이었던 날의 사전 조건을 학습해, 다음 추세일을 장 초반에 더 정확히 예측"하고, 새 데이터가 쌓일 때마다 시스템이 스스로 개선되는 구조.

### 8.1 라벨링 파이프라인 (과거 데이터 분석)

매 거래일에 대해 다음을 자동 산출·저장:

**라벨 (그날의 결과)**
- DC1, DC2, 일간 등락률, 갭, 장중 등락률, 최대 역행폭(MAE)
- 3클래스: 상방추세일 / 하방추세일 / 비추세일

**피처 (그날 아침에 알 수 있었던 것 — 예측 입력)**
| 그룹 | 항목 |
|---|---|
| 매크로 | 미 2y/10y 전일 변동·5일 추세, USD/KRW 수준·전일 변동, WTI, 기대인플레 프록시 |
| 해외장 | 나스닥·SOX 전일 등락, 나스닥 선물 아침 방향, 니케이 선물 아침 방향 |
| 모멘텀 | 하닉/삼전 전일·전전일·3일 누적 등락, 신고가 근접도, 연속 상승/하락 일수 |
| 수급 | 전일 외인 현물 순매수, 20일 평균 대비 배율, 외인 선물 포지션 변화, 리밸런싱 캘린더 플래그(C2) |
| 이벤트 | NFP/CPI/FOMC/실적/만기일 D-day 플래그, 전일 서프라이즈 방향 |
| 장초반 (09:30 시점 추가) | 갭 크기·방향, 첫 30분 방향, FKS200 초반 기울기, 10시 외인 매도 페이스(L5 3요소) |
| 원인 주석 | **사용자 수동 입력**: 그날의 지배 재료 분류 태그(전쟁/관세/실적/수급/소송/AI뉴스 등) — 6/23=지정학, 7/2=소송·메타 같은 정성 라벨 |

### 8.2 학습 단계 (데이터 양에 맞는 점진 고도화)

**Stage 1 — 규칙 발견 (표본 ~60일)**: 조건부 통계표. "환율 하락 + 전일 3일 누적 -10% 이하 → 상방추세일 확률 X%" 식의 단순 크로스탭. 해석 가능하고 과적합 위험 낮음. 산출물은 사람이 읽는 월간 패턴 리포트
**Stage 2 — 확률 모델 (표본 120일+)**: 로지스틱 회귀 or 얕은 그래디언트 부스팅으로 3클래스 확률 P(상방추세/하방추세/비추세) 예측. 08:30 버전(장전 피처만)과 09:30 버전(장초반 피처 포함) 두 모델 운용
**Stage 3 — 임계값·파라미터 최적화**: θ(10분봉 DC1 60%, 봉 주기 포함), 갭 기준(±2%), 과대낙폭(-12%), T-스코어 컷 등을 그리드 탐색. **최적화 목표는 적중률이 아니라 "거래비용 차감 후 순손익 × 최대낙폭 페널티"** — 적중률 최적화는 작은 이익 다수 + 큰 손실 소수 함정에 빠짐

### 8.3 지속 학습 루프 (운영 사이클)

```
[매일 15:40 장후 배치]
  1. 당일 5분봉 수집 → DC1/DC2 계산 → 라벨 확정
  2. 아침 예측값 vs 실제 라벨 대조 → 예측 로그 기록
  3. 피처 스냅샷 저장, 사용자에게 "원인 주석 1줄" 입력 요청 (텔레그램)
  4. 추세일이었는데 예측 실패한 날 → '오답 노트'에 자동 등재, 주말 리뷰 대상

[매월 1회]
  5. 워크포워드 재학습: 직전 N개월로 학습 → 다음 달 검증 (미래 데이터 누출 금지)
  6. 챔피언/챌린저: 신규 모델은 1개월 페이퍼 예측으로 기존 모델과 병행 비교 후 성능 우위 시에만 교체
  7. 파라미터 재탐색 (θ 등) 및 민감도 리포트

[분기 1회 or 레짐 변화 감지 시]
  8. 드리프트 점검: 최근 승률·손익비가 학습기 대비 통계적으로 악화되면 경보
     (예: 환율 레짐 전환, 외인 매도 국면 종료, 금리 사이클 전환 시 과거 패턴 무효화 가능)
  9. 무효화된 규칙은 삭제가 아니라 '레짐 조건부'로 보존 (예: "고환율 레짐에서만 유효")
```

### 8.4 학습 시스템의 한계와 안전장치
- **표본 부족이 최대 적**: 추세일은 한 달에 3~9일. 1년 모아야 표본 ~60개. Stage 2 이전에 복잡한 모델(딥러닝 등) 금지
- **예측 확신도와 비중 연동**: P(추세) 60%대 = 1/3 비중, 80%+ = 정상 비중. 모델이 틀려도 살아남는 구조가 모델 성능보다 중요
- 학습 결과가 어떤 경우에도 하드 블록(X·XS 예외 필터)과 리스크 규칙(R1~R8)을 무효화할 수 없음 — 학습은 "언제 진입할까"만 개선하고, "얼마나 잃을 수 있나"는 규칙이 고정

---

## 부록 A. 성공사례 원형 (2026.6월 NFP 트레이드 — 규칙의 출처)

전개: 고환율·금리상승·NFP 대기 + 메타 AI 임대 뉴스로 하닉 연속 급락 → NFP가 컨센서스 대비 크게 부진 → 금리인상 가능성 후퇴로 안도 국면인데도 금요일 장 초반 추가 눌림(비정상) → FKS200 하락폭 축소 후 상승 반전 확인 → 약 -5% 구간에서 레버리지 매수(1.7억) → 시세포착 -3% 설정 → 상승에 따라 손절선 상향 수정 → 종일 상승, +24% 매도.

추출된 교훈: ① 외국 지수선물은 방향을 잡으면 지속하는 경향 → 반전 트리거로 사용 ② 매크로 개선 + 가격 하락의 괴리가 최고의 진입 신호 ③ 손실제한은 진입과 동시에, 수정은 이익 방향으로만.

---

## 부록 B. 사용자 개정 이력 (스펙 대비 변경분 — 코드가 이 개정을 따른다)

### 2026-07-07 — Bias(2.2)·레버리지 셋업(4.1) 개정
- **C5 밤사이 미국장(나스닥·SOX) Bias에서 제외** — 미국장은 이미 금리·서프라이즈에 반영된
  결과 변수라 이중 계산. (셋업 L10 자동 근사와 S2 매크로 악화 판정에서는 계속 사용)
- **C4 미 금리 10년물 → 2년물** — 정책 민감도가 높은 2Y가 방향을 선행. 소스는 네이버
  US2YT=RR 일봉, 눈금은 %p (레짐 ±0.03%p, 전환 감지 5일 ±0.08%p·전일 ∓0.03%p).
- **지표 서프라이즈(L10)를 Bias 방향 결정에 가중 2배** — NFP·CPI·FOMC 등 정성 이벤트가
  방향 결정력이 가장 크다는 성공사례 원형(부록 A) 반영.
- **레버리지 셋업 L2(금리·환율 안정) 필수에서 제외** — Bias C3·C4에서 이미 반영, 셋업에서
  재요구하면 이중 계산. 행은 참고 표기로만 유지.
- **실적 발표 선반영 원칙** — 삼전·하닉 실적은 주가에 선반영되는 경향. AI 정성 판정(L7·L8)은
  발표 수치의 절대 호악이 아니라 컨센서스 대비 서프라이즈와 선반영 소화 여부로 판단
  (autoAnnotate 프롬프트에 명시).
- RV1 분봉 모멘텀 신설은 확장기획서 9장 참조.

### 2026-07-09 — 수급 연동·스윙 추세·매크로 게이트 개정

**축 1 (Bias)**
- **C2 리밸런싱 캘린더는 필수 아닌 옵션** — 방향 투표에서 제외(가중 0), 참고 표기만.
- **L7 재정의** — 낙폭이 없어도 **매일** 전일 미국 뉴스·주식영향 정보를 AI가 분석해 영향도
  (상방/하방/중립)를 Bias에 반영 (autoAnnotate `us_news_impact`·`us_news_note`, 마이그레이션 020).
  낙폭 원인의 비실적 여부는 셋업 가점 L7·XS2 차단에서 계속 사용.
- **L8(이익 컨센서스) 제거** — Bias·셋업 가점 모두 제거 (롱 가점 만점 11→9). XS2 차단 판단에는 유지.
- **C5 중 SOX(미 반도체)만 재도입, 가중 2배** — "SOXX를 나스닥보다 더 중요하게. 장중엔 FKS200,
  장후엔 SOXX가 주요 판정 데이터." 나스닥은 이중 계산이라 계속 제외. L10 자동 근사·S2 매크로
  악화 판정의 해외장 항목도 나스닥 → SOX 우선으로 교체.

**축 2 (Confirmation)**
- **T4(외인 선물)·T5(프로그램) KIS 연동 완료** — 시장별 투자자매매동향(FHPTJ04030000,
  코스피 KSP/0001·선물 K2I/F001)·프로그램매매 종합현황(FHPPG04600101). 단위 억원(백만원÷100).
  T4는 부호가 아닌 **30분 기울기 ±500억**으로 판정 (2.5.2의 기울기 원칙).
- **T8 재정의 (중요, 가중 1→2)** — 거래대금 확장 → **외인 현물+프로그램 수급 흐름(30분 기울기
  ±150억)**. 순매도라도 감속이면 매수기회 방향, 순매수라도 감속이면 매도기회 방향.
- **T6 재정의 (스윙 구조, 가중 +2 점수화)** — 5분봉 전환 횟수(3회↑=횡보)는 폐기. 산과 골의
  연결선으로 판단: ①고점 2개 연결선과 저점 2개 연결선이 **같은 방향이면 추세** ②불일치면 다음
  산·골까지 대기 후 **고점 3개(또는 저점 3개) 연결선의 지향 방향** ③3점으로 불가하면 4점
  ④4점까지도 불일치·평탄이면 그때만 **횡보일**. 이것은 "변동성의 추세"이며, 판단은 개장 초반
  한정이 아니라 **13:30까지 새 산·골이 생길 때마다 재평가**. '미정'(스윙 부족)은 횡보가 아님 —
  성급한 횡보일 선언 방지.

**셋업·게이트**
- **LM 매크로 게이트 신설 (레버리지 필수)** — 매크로가 제일 중요. 매크로 악화(2Y 금리↑·환율
  +0.3%↑·SOX -0.5%↓ 중 2개 이상)면 **차트(축2)가 좋아도 레버리지 진입 금지** — 매크로가 널뛰면
  주가도 널뛰어 변동이 너무 큼. V반등 조기 선진입에도 동일 적용.
- **L1(Bias 상방) 필수에서 제외** — 참고 표기만. 매크로 컨디션은 LM이 담당, Bias는 비중 결정(R5)에 사용.
- **S5(디커플링 과열) 제거** — 참고 표기만 (숏 가점 만점 5→3).

**알림**
- **외인·프로그램 수급 반전 문자 신설** — 코스피 외인 현물·프로그램 누적 순매수가 당일 극값
  대비 800억 이상 되돌아오면 발송 (순매도 감속=매수기회 / 순매수 감속=매도기회 관찰).
  극값 에피소드 재무장 방식. 시간별 동향은 대시보드 T4·T5·T8 상세로 확인.
- **급등·급락 절대 단계는 당일 극값 갱신 중일 때만 발송** — 실측 오류 수정 (09:56 고점 4%대에서
  +2.7%로 하락 중인데 "급등 +2.7%" 발송됨). 하락 중의 낮은 단계 재돌파는 급등이 아니라 반락이며
  스윙 알림이 담당.
- **판정 확정 문자는 제목에 "*판정 확정" 표기** — 다른 문자와 구분 (제목은 장문 LMS에만 붙음).

**기타**
- `/signal/guide` 신설 — 축1·축2·D·DC·셋업·리스크·알림 전 코드를 드롭다운(아코디언)으로 설명.
- DC2 정의 확인: 0~1 효율비(경로 대비 순이동), **-1 없음** — 1에 가까울수록 추세 강함.

### 2026-07-10 — 외인 현물 게이트·장중 진입 브리핑 (사용자 지정)

**셋업·게이트**
- **FG 외인 현물 게이트 신설 (레버리지·인버스 양쪽 필수)** — 장중 외국인 코스피 현물 순매수를
  주요 팩터로 진입 판단에 직접 반영. 판정은 T8과 같은 눈금(30분 기울기 ±150억, `trend.t8MinDelta30`):
  - 레버리지: 외인 현물 흐름이 **뚜렷한 이탈(Δ30분 ≤ -150억)이면 필수 미충족**.
  - 인버스: 외인 현물 흐름이 **뚜렷한 개선(Δ30분 ≥ +150억)이면 필수 미충족**.
  - 데이터 부재·중립(±150억 미만)은 통과 — 수급은 차단 조건이지 단독 진입 근거가 아니며,
    KIS 미연동 환경·과거 백테스트(수급 데이터 없음)의 판정을 바꾸지 않는다.
  - 점수(T8 가중 2)와 게이트(FG)는 역할이 다름: T8은 추세 확신도에 기여, FG는 반대 수급일 때 진입 자체를 막는다.

**알림 — 장중 진입 브리핑 (LMS)**
- **고정 체크포인트**: 개장 후 **+1·3·5·10·15·20·30·50분** 8회. 수집이 늦게 시작되면 지난
  체크포인트는 건너뛰고 유예(10분) 내 최신 것만 발송 (몰아서 여러 통 방지).
- **09:50 이후**: ①추세 **전환**(직전 브리핑의 30분 기울기와 부호 반대, 양쪽 |기울기|≥0.15%p)
  ②추세 **감속**(같은 방향인데 현재 30분 기울기가 직전 브리핑의 50% 이하, 직전 ≥0.3%p)
  ③판정(dayType) 변경 — 시 즉시 발송 (직전 브리핑과 최소 15분 간격).
  해당 없어도 **직전 브리핑 후 60분 경과 시 정기 발송**. 기울기 기준 시계열은 FKS200(K200 선물 등락률).
- **본문**: 판정 요약(dayType·방향·T점수) + 장중 수급(외인 현물 누적·Δ30분, 프로그램, 선물 외인)
  + 지표 9종 — **미2Y·미10Y(%p 차), 환율, WTI, K200선물(FKS200), 닛케이(JP225), SOXX,
  나스닥선물(NQ), S&P500선물(ES)** — 각각 **직전 브리핑 값 대비 변화율**(첫 회는 전일 종가 대비) 병기.
  미국 현물(SOXX)은 한국 장중엔 시간외 마지막 값이라 변화가 0에 가까울 수 있음(표기는 유지).
  닛케이는 당일 체결 가드(휴장이면 휴장 표기). 지표 9종이라 장문(LMS) — 단문 정책의 예외.
- 발송 트리거는 기존과 동일하게 `/api/signal/state` 호출(60초 폴링·외부 크론)에 얹힌다 —
  상주 프로세스가 없으므로 페이지가 닫혀 있고 외부 크론도 없으면 체크포인트를 놓칠 수 있다.
### 2026-07-13 — FKS200 급변·스윙 알림 간격 20% 축소 (사용자 지정)

**알림**
- **K200 선물(FKS200) 급변 절대 단계 0.7 → 0.56%p 등간격** (`moveAlert.futLevels`) — 선물 1%가
  주식 약 2%에 해당해 변화폭이 큼 → 발송 구간 20% 축소. 상한은 9.8 → **12.32**로 연장
  (2026-07-13 실측: 장중 -9.8% 도달로 기존 상한 소진).
- **선물 반락·반등 스윙 스텝 0.7 → 0.56%p** (`moveAlert.futSwingStep`) — 같은 20% 축소.
  하닉·삼전(±3/5/7/10%, 스윙 1.5%p)은 변경 없음.

**버그 수정 — KIS 수급이 사흘간 전부 null이었던 원인 (T4·T5·T8 "데이터 대기")**
- 원인: 마이그레이션 019(`hynix_vol`)가 프로덕션 DB에 미적용 → 틱 INSERT가 매번 컬럼 오류 →
  `appendTick` 폴백이 신규 컬럼 5개(019+020)를 통째로 제거하고 재시도 → **KIS 호출은 성공하는데
  수급 4컬럼이 전부 null로 저장** (7/9~7/13 실측. 프로덕션 KIS 연결 자체는 /api/debug/kis로 정상 확인).
- 수정: 폴백을 정밀화 — 오류 메시지에 명시된 **없는 컬럼만** 빼고 재시도 (최대 6회 반복).
  019 적용 전에도 KIS 수급은 저장된다. 거래량 급증 알람(hynix_vol)은 019 적용 후 활성화.
- 같은 이유로 거래량 급증 알람도 그간 미발동이었음 (5분봉 거래량 이력이 쌓인 적 없음).

**아침 브리핑 지연 보정**
- Vercel Hobby 크론은 지정 시각 대비 34~54분 지연 실측 (08:30 지정 → 09:04·09:24·09:04 발송,
  개장 후 도착) → 스케줄을 07:45 KST로 당겨 지연 포함 08:00~08:40 도착으로 보정.
- 정확한 08:30 발송이 필요하면 cron-job.org에 08:30 작업 추가 (중복 방지 있음 — 병행 안전).

**축1(Bias) 확장 — C6~C9 신설 (사용자 지정)**
- **C6 미 금리(10Y)** — C4(2Y·정책 민감)와 별개의 장기 할인율 축. 전일 ±0.03%p 기준 (야후 ^TNX).
- **C7 WTI 유가** — +2% 이상 급등=물가·금리 압력 하방 / -2% 이하 급락=완화 상방, 그 사이 중립.
- **C8 달러지수(DXY)** — ±0.3% 기준. 강달러=외인 수급 역풍 하방. C3(원/달러)의 글로벌 공통분을 분리 관찰.
- **C9 미 국채가격(TLT)** — 참고 표기만(가중 0): C6(10Y 금리)과 역방향 동일 정보라 이중 계산 방지.
- 백테스트 시나리오엔 데이터가 없어 '미상'(투표 미참여) — 과거 판정 회귀에 영향 없음.

**C6 미 금리(10Y) 레벨 구간 판정 (사용자 피드백 2차)**
- "4.57%면 중립이 아니라 중립에서 위험으로 진행" — 전일 변화만 보던 판정에 절대 레벨 구간 추가:
  - **안정 <4.5%**: 전일 ±0.03%p 기준으로만 상방/하방.
  - **경계 4.5~4.8%** (`us10yWarn`): 상승 중이면 폭과 무관하게 **하방** ("위험 방향 진행"),
    -0.03%p 미만 하락이면 상방.
  - **위험 ≥4.8%** (`us10yDanger`): 뚜렷한 완화(-0.03%p↓)가 아닌 한 **하방**.

### 2026-07-13 (2차) — 인버스 셋업 자동화·판정 정합·추세일 보수 게이트 (사용자 지정)

**인버스 셋업**
- **S6(악재 뉴스 +2) 자동화** — 수동확인 제거. 매일 AI 주석으로 자동 판정: 전일 미국 뉴스
  하방(L7) 또는 낙폭 원인 주석 존재(하락+원인 분석됨)면 충족. 사용자 직접 입력은 계속 우선.
- **S7(금리 우려 유지 +1) 자동화** — 2Y 레짐 상승 또는 5일 +0.08%p 초과 추세, 또는
  10Y 경계 구간(4.5%↑)에서 완화 없음이면 충족 (C4·C6 데이터 재사용).
- **verdict 정합 수정** — 하락 '추세일' 확정이면 가점 미달이어도 진입후보. 실측 사고(7/13 11:48):
  강한 하락일에 필수 S1~S4 전부 충족인데 S6·S7이 수동확인(null)이라 가점 0/3 → 카드가 '대기'
  — 판정(추세일_하방·인버스 검토)과 표시가 상충했음. 가점이 영원히 0인 구조였던 것도 원인.

**추세일 확정 보수 게이트 (`trend.strongDay`)**
- "매크로 방향성이 뚜렷한 날, 분봉에서 변동성보다 추세가 센 날에만 추세일 판정 — 변동성이
  심할 때는 보수적으로" (사용자 지정). T-스코어가 추세일이어도:
  ① 축1 매크로가 **같은 방향 + 강도 2 이상** 정렬, ② **DC2 ≥ 0.35** (변동성 대비 순추세 효율)
  — 둘 다 충족해야 추세일 유지. 미충족이면 **약한추세 강등** (1/3 비중·타이트 트레일링 -2%,
  risk·셋업·표시 모두 강등 후 등급 기준). 강등 사유는 extNotes·dataNotes에 표기.

**C6 미 금리(10Y) 레벨 단계 사다리 (사용자 지정 3차)**
- 기존 2단(경계 4.5 / 위험 4.8)을 4단 사다리로 세분화 (`us10yBands`) — 단계가 오를수록
  하방 판정 조건이 느슨해져 위험 수위가 단계별로 상승:
  - **안정 <4.55%**: 전일 ±0.03%p 기준으로만 판정.
  - **경계 4.55~4.57%**: 상승 중이면 폭과 무관하게 하방.
  - **주의 4.57~4.59%**: 보합(전일 -0.01%p 이상)도 하방.
  - **위험 4.59~4.62%**: 뚜렷한 완화(-0.03%p↓)가 아닌 한 하방.
  - **최고위험 4.62%↑**: 완화 중이어도 하방 + **가중 2** (레벨 자체가 위험).
- S7(금리 우려 유지) 자동 판정의 10Y 기준도 첫 단계(4.55%)로 연동.

**보유 종목 매매 판단(장중 시황 요약) 급락일 오판 수정 — 별도 모듈(holdingScore)**
- 실측 사고(7/13): 하닉 -16%·코스피 -8.9% 폭락일에 "삼전 7.비중확대 / 하닉 6.중립(매수우위)".
- 원인 분해(실데이터 재현): 당일 등락 감점 상한 **-6** + 장세 -8 = 위기 감점 최대 -14인데
  펀더멘털 가점(EPS리비전 +16·컨센 +8·목표가 +10 등)이 +50~70으로 항상 압도. 게다가
  '목표주가 여력'은 급락할수록 가점이 커지는 역설. marketDrop(시장 최악 %)은 계산만 하고 미사용.
- 수정: ①당일 등락 사다리 확대(-1.5/-3/-5/-8/-12% → -3/-7/-12/-18/-24) ②시장 급락 팩터 신설
  (marketDropPct 전달, 최대 -12) ③장세 상단 세분화(리스크 80↑ = -15) ④급락일엔 컨센서스·
  목표주가 가점 절반(낡은 앵커) ⑤최종 안전장치 — 스탠스 상한: 종목 -5%/시장 -4%면 최대
  5(중립·매도우위), 종목 -8%/시장 -6%면 최대 4(비중축소). 재현 검증: 같은 날 데이터로
  삼전 7→4, 하닉 6→3.
- 진단 도구: `npx tsx scripts/stance-repro.ts` — 삼전·하닉 요인 분해 출력.

**보유 매매 판단 캘리브레이션 루프 신설 (사용자 지정)**
- `stance_snapshots` 테이블 (마이그레이션 021 — 수동 적용 필요): 매일 15:40 signal-eod 크론이
  종목별 스탠스·점수·요인 분해를 저장하고, 다음 거래일 EOD에 그날 수익률(next_day_pct)을 백필.
- 리포트: `npx tsx scripts/stance-error-report.ts [일수]` — ①스탠스 구간별 익일 평균 수익률
  ②오판 사례(매수판정→익일 -3%↓ / 매도판정→익일 +3%↑) ③오판 기여 요인 랭킹.
- 표본이 쌓이면 랭킹 상위 요인의 기준값(사다리·상한·가중)부터 조정 — DC1 θ 그리드 탐색(2.5.6)과
  같은 원칙: 라벨(실제 수익률)은 고정 잣대, 판정 파라미터만 움직인다.

**전 문자 시각 표기 (사용자 지정)**
- 반락·반등 스윙 "(-12.2%p, 14:23)" · 수급 반전 "(...億, 10:50)" · 분봉 모멘텀 "[10:31]" ·
  판정 확정 "(T 8/13·DC1 65%·10:31)" — 급변·거래량은 기존부터 표기.
- 배경(7/13 실측): 삼전·선물 반락 문자가 2분 간격으로 같은 값("고점+2.5%→-9.7%")으로 도착해
  오류처럼 보임 — 실제로는 각자 시계열이 정확했고(삼전 고점 +2.52·선물 +2.54, 14:23/14:25 시점
  각각 -9.65/-9.66) 반올림 우연 일치. 시각 표기로 구분 가능해짐.

**RV1 반복 발송에 '추가 진행' 조건 (사용자 피드백)**
- 실측(7/13 14:38·14:44): 같은 반등을 서로 다른 조건이 재감지 — "1분봉5개 +1.7%p"와
  "5분봉 +1.7%p"는 **같은 5분 구간의 두 잣대**인데 쿨다운(5분)만 지나면 (2차)(3차)로 중복 발송됨.
- 수정: 반복(2·3차)은 직전 발송 시점의 하닉 등락률 대비 그 방향으로
  **`repeatMinProgressPct`(0.8%p) 이상 추가 진행**했을 때만. 발송 시점 등락률을
  market_snapshot.levelPct로 기록해 다음 반복 판정에 사용 (기록 없는 과거분은 쿨다운만 적용).
- 하락 추세일의 상승 모멘텀 문자 자체는 유지 — "추세를 조건으로 하지 않는다"(사용자 확정
  2026-07-07 3차)는 원칙 그대로, 반복의 '새 정보' 요건만 추가.

**RV1 품질 필터 3종 (사용자 피드백 3차 — "레버리지 판단을 하지 말았어야")**
- 실측(7/13): 하락 추세일 14:00 이후 윗꼬리 긴 5분봉 반등의 반복에 레버리지 3차까지 발송,
  15:05 재급락. 세 겹의 허점을 각각 차단:
  1. **진입 시간대 게이트** — 발송은 판정 구간(09:30~13:30)만. L4 신규 진입 마감 후의
     "진입 검토" 문자는 모순 (기존엔 장전·마감만 제외 → 오늘 3통 모두 14시 이후였음).
  2. **추세일 정합 게이트** — 추세일 '확정'과 반대 방향이면 발송 안 함 (하락 추세일 확정 중
     레버리지 문자 금지, 반대도 동일). 약한추세·비추세 변동장은 기존 원칙(방향 무관,
     사용자 확정 2026-07-07) 유지. 진짜 추세 전환이면 판정 자체가 바뀌며 브리핑이 알림.
  3. **윗꼬리 필터** (`wickRetraceRatio` 0.4 · `wickRetraceMinPp` 0.3) — 신호 창 극값 대비
     변동폭의 40% 이상 되돌린 상태의 신호는 반대 세력에 흡수된 모멘텀으로 보고 무효
     (retracePp를 reversal 결과에 기록).
- 시뮬레이션 검증 5종: 깨끗한 반등 발송 / 윗꼬리 차단 / 14시 차단 / 추세일 반대 차단 /
  추세일 같은 방향 발송 유지. 백테스트 회귀 0건.

## 부록 C — 미국 반도체 레버리지/인버스 신호 (2026-07-13 사용자 지정)

**대상·기준 지수**
- 레버리지 **ProShares USD**(2x 반도체) / 인버스 **SSG**(-2x). 판정 기준 지수는 **SMH** —
  2년 일간 실측(scripts/us-signal-calibrate.ts): USD와 상관 **0.9547·β 2.04** (후보 중 최고.
  ^SOX 0.928·β1.83 / SOXX 0.905 / NQ 0.878). USD의 공식 기초지수(DJ U.S. Semiconductors)는
  실시간 소스가 없어 최고 상관 ETF로 대체. SMH가 한국의 FKS200 역할.

**구조 — 한국 M7 재사용**
- 미국 정규장(09:30~16:00 ET, 390분)을 한국 세션(09:00~15:30 KST, 390분)에 1:1 매핑
  (개장=540분) → **축2는 computeTrend 그대로 재사용** (SMH→futPx). 수급(T4·T5·T8·L5)은
  소스 부재로 자동 미산출(만점 제외). 관찰 30분·진입 마감(개장+240분=13:30 ET)·당일 청산 대응.
- 축1(usBias)은 미국 세션 특성 반영 — 매크로가 장중 실시간으로 움직이므로 라이브 투표:
  U1 10Y(레벨 사다리 us10yBands 재사용) · U2 DXY ±0.3% · U3 WTI ±2% · U4 VIX(±8%·레벨 30)
  · U5 나스닥선물 ±0.5%. LM 게이트: 10Y↑·DXY↑·VIX↑ 중 2개 이상이면 USD 금지.
- 보수 게이트·XS1(직전 1~3일 -9% 폭락 후 인버스 금지)·X1(큰 갭 4% 추격 금지) 동일 원칙.

**임계값 — SMH 실측 도출 (37거래일 5분봉 + 2년 일간, 추세일 12일 추출)**
| 항목 | 값 | 근거 |
|---|---|---|
| DC 판정(5분봉) | θ 0.55 · DC2 0.14 | 추세일 DC1 중앙 0.56/횡보 0.51 · DC2 추세 25% 0.16/횡보 75% 0.08 |
| DC 라벨(10분봉) | θ 0.60 · DC2 0.18 | 추세 중앙 0.62/횡보 0.54 · DC2 추세 25% 0.23/횡보 75% 0.11 |
| 보수 게이트 DC2 | 0.20 | 추세일 5분 DC2 중앙값 |
| 급변 스텝 | 1.1% (상한 13.2) | 일간 \|등락\| 중앙 1.35%의 0.8배 (한국 공식 동일) |
| 갭 | 무갭 0.4 · 큰갭 4% | \|갭\| 중앙 1.98 · 90% 4.86 |
| 폭락/과열 | -9% / 5일 +12%·3연속 | 일간 99% ~8% + 여유 |

**운영**
- 수집·발송: cron-job.org가 `/api/signal/us/state?secret=`을 한국 야간(서머타임 22:25~05:05 KST,
  겨울 23:25~06:05) 1분 간격 호출. 마이그레이션 022(us_signal_ticks) 필요.
- 페이지: `/signal/us` (홈 > 시장·알림·분석 > 레버리지·인버스 신호 (미국)).
- 문자: `us_trend_up`(USD 검토)·`us_trend_down`(SSG 검토) — 판정 확정 + 게이트 통과 시 1일 1회,
  SMH 급변·반락·반등 스윙(1.1% 스텝, 에피소드 재무장). 중복 방지는 ET 거래일 기준.
- 다음 단계(미구현): RV1형 분봉 모멘텀(미국판)·EOD 라벨 확정 배치·스탠스 캘리브레이션 연동.

**조용 시간 (사용자 지정 2026-07-13)**
- 미국 신호 문자(SMS)는 **01:00~07:00 KST 억제** — 이메일은 발송, 수집·판정은 계속
  (`US_SIGNAL_CONFIG.quietSms`, dispatch `suppressSms`). 크론은 장 마감(05:00 KST)까지 돌려
  시계열을 온전히 축적. 조용 시간에 소진된 alertKey는 1일 1회 원칙에 따라 이후 문자 재발송 없음
  — 밤사이 신호는 아침에 이메일로 확인.

**알림량 절감 (사용자 지정 2026-07-13 3차)**
- **이메일 1/3 축소** — 심각도 high와 브리핑류(intraday_summary)만 이메일 발송, medium·low는
  SMS만 (`email:cut` 기록). 실측 5일 167건 중 high 63건(38%) ≈ 1/3. 조용 시간(suppressSms)엔
  이메일이 유일한 채널이라 심각도 무관 발송.
- **삼전·하닉 개별 종목 문자 50% 축소** — FKS200(선물) 문자가 이미 커버하므로:
  급변 단계 ±3/5/7/10% (4단계) → **±4/8/12%** (간격 2배·12 연장은 당일 하닉 -16% 실측),
  스윙 스텝 1.5%p → **3%p**. 선물(FKS200) 문자는 변경 없음.

**부록 C 추가 — TV 거래량 확인 신호 (사용자 지정 2026-07-13 2차)**
- 미국엔 한국식 수급(T4·T5·T8, 만점 6점)이 없어 축2 미산출이 많음 → **거래량 확인(TV, 가중 2)**
  신설: 당일 방향 5분봉의 거래량 합이 반대 방향 봉의 **1.3배 이상**이면 '거래량이 추세 동반' 충족.
  추세일은 방향 봉에 거래량이 실리는 고전적 확인 지표. SMH 누적 거래량 (마이그레이션 023,
  미적용 시 TV만 미산출 — INSERT는 없는 컬럼만 제거 후 재시도).
- TV 반영 후 등급을 computeTrend와 동일 규칙으로 재산출 → 보수 게이트 적용 순서 유지.
- 축1 지표엔 실제 레벨 병기 (환율 원·2Y/10Y %·WTI $·DXY·VIX·NQ 지수·TLT $).

**부록 C 추가 — RV1 미국판 (사용자 지정 2026-07-13 3차)**
- SMH 분봉 모멘텀 즉시 신호 — 상승=USD(2x)·하락=SSG(-2x) 검토 문자. 감지 엔진은 한국
  detectReversal 재사용 (시리즈·임계값 주입, 한국 동작 불변).
- 임계값 = SMH 실측 |변화| 99% 분위 (1분봉 6일 1,943표본): 1분봉 1개 0.35 / 3개 0.55 / 5개 0.7,
  5분봉 0.7 / 1.2 / 1.5 / 1.8 %p.
- 발송 정책·품질 필터는 한국과 동일: 진입 시간대(개장+30분~+240분)만 · 추세일 확정 반대 방향
  차단 · 윗꼬리(변동폭 40%·최소 0.2%p) · 같은 방향 3회/일 · 쿨다운 10/5분 · 반복은 직전 발송
  대비 +0.4%p 추가 진행 시에만. 중복 창은 ET 거래일. 조용 시간엔 이메일만.

**부록 C 추가 — 확정 문자 지속 조건·해제 문자 (실측 2026-07-13 23:05)**
- 사고: 반등 중 T3가 한 틱 통과 → 5/8=62.5%로 확정 문턱을 넘어 "추세일 하방 확정" 문자 발송 →
  다음 틱 반등 확대로 비추세 회귀, 정정 없음. 미국은 가용 신호가 적어(8~12점) 1점 신호 하나로
  문턱을 넘나드는 채터링에 취약.
- 수정: ①확정 문자는 `confirmPersistMin`(5분) 전 시점 판정도 같은 방향 '추세일'일 때만 발송
  (틱 시계열을 잘라 과거 시점 판정을 재계산 — 무상태). ②확정 문자 발송 후 현재·5분 전 모두
  추세 훼손이면 **해제 문자** 1회 ("하방 확정 해제 — 추세 훼손, SSG 보유 시 점검").

**부록 C 추가 — T6 스윙 구조 피벗 보정 (사용자 지정 2026-07-13 4차)**
- 산·골 연결선 규칙(T6·횡보일 선언)은 한국 엔진 재사용으로 처음부터 미국에 적용돼 있었으나,
  피벗 확정 반전폭이 K200 눈금(0.2%) 그대로였음. SMH 실측(30일 5분봉): 0.2%면 일평균 피벗
  17.9개(노이즈 과다), **0.4%면 9.1개**로 K200 평상시 밀도와 유사 → `us.swing`
  { minAmpPct 0.4, tolPct 0.3 }으로 보정 (computeSwingStructure에 주입점 추가 — 한국 불변).

**부록 C 추가 — 공유 엔진 눈금 전수 감사·보정 (사용자 지정 2026-07-13 5차)**
- "FKS200 기준 값들을 전부 SMH에 맞게" — computeTrend 내부의 %기준 상수를 전수 감사해
  스케일 민감 항목 4개를 주입점(`opts.scale`)으로 분리, 미국은 SMH 변동성(K200의 ~2배) 비례 보정:
  | 항목 | K200 | SMH |
  |---|---|---|
  | 당일 방향 형성 대역 (dayDir) | ±0.1% | ±0.2% |
  | T7 무갭 컷 | 0.15% | 0.4% |
  | 장중 재형성 최소 순이동 | 1.0% | 2.0% |
  | 재형성 창 방향 대역 | ±0.05% | ±0.1% |
- 시간 기반(ORB 30분·T1 유지 30분·재형성 창 90분)과 비율 기반(T2 80%·T3 40%·DC θ)은
  눈금 무관이라 그대로. O1 시가 유형(ext.o1)은 기록 전용·기본 OFF라 보정 보류.
- 이로써 미국 눈금 보정 완료 항목: DC(0.55/0.14)·스윙 피벗(0.4/0.3)·급변 스텝(1.1)·갭(0.4/4)·
  폭락(-9)·과열(+12)·RV1(99% 분위)·보수 게이트 DC2(0.2)·엔진 scale 4종. 한국 동작 전부 불변.

### 2026-07-14 — 스윙(산/골) 문자 민감도 복원 (사용자 지정)
- 7/13 발송량 축소의 의도 명확화: **추세 판정과 중복되는 문자만 축소**하고, 별도 지정했던
  기능성 문자는 원래대로.
  - 삼전·하닉 **급변 절대 단계 4/8/12 유지** — FKS200 급변 문자와 같은 '추세 수준' 정보라 중복.
  - 삼전·하닉 **반락·반등 스윙 1.5%p 복원** (3.0 → 1.5) — 반전 감지는 등락률 문자로 안 보이는
    별도 기능 (사용자 확정 2026-07-08 원값). 선물(FKS200) 스윙 0.56%p는 변경 없음.
  - 이메일 절감 규칙(high·브리핑만)은 유지.

### 2026-07-15 — 셋업 필수/가점 재설계·점수 레인지 확대 + 알림·브리핑 정비 (사용자 지정)

**셋업 재설계 — "진짜 필수만 필수로"**
- 원칙: 필수(절대 조건)는 **방향·시간대·차단형 게이트**만 — 이것이 없으면 논리적으로 진입이
  성립하지 않는 것들. 확률·확신도를 높이는 조건은 전부 배점 차등 가점으로.
- **중대 발견**: L5(하닉 외인 잠정 수급)는 네이버 종목 trend API가 당일 행을 장중 제공하지 않아
  (첫 행이 항상 전일) **가동 이래 단 한 번도 산출된 적 없음** → 필수로 묶여 있어 프로덕션에서
  레버리지 '진입후보/강한신호' 확정이 구조적으로 불가능했다 (백테스트는 합성 잠정치를 주입해 통과).
- 레버리지: 필수 = LM·FG·L3·L4. 가점(만점 14) = L5 +3(필수→가점) · L6 +3 · L1 +2/+1(참고→가점) ·
  L7 +2 · L10 +2 · L2 +1(참고→가점) · L9 +1. L11은 소스 없음으로 제거.
  후보 6점↑ · 강한 10점↑ (기존 만점 9 → 14로 레인지 확대). 상방 추세일 확정이면 가점 미달이어도
  후보 (숏과 대칭 — 판정-카드 정합).
- 인버스: 필수 = FG·S3·S4. 가점(만점 8) = S2 +3(필수→가점) · S1 +2(필수→가점) · S6 +2 · S7 +1.
  후보 4점↑. XS1·XS2 하드 블록 불변.
- L5·L9 부활 경로: KIS 종목별 투자자매매동향 연동 (후속 작업).

**알림·판정 정비 (같은 날)**
- 시초 2분 스테일 틱 가드 / 삼성전자 스윙 문자 중단 / 수급 반전 극값 ±500억 조건 /
  축1·축2 상충(약한추세+반대 Bias 강도2↑) 시 비추세 강등 (한·미).
- 장중브리핑: K200 선물 틱 소스 통일·NQ/ES/WTI 신선도 가드·삼전 등락 추가.
- 아침브리핑: 야간선물 등락률 파싱 실패 시 ? 표기·발표 지난 지표 제외·AI 부호 해석 규칙.

**L5·L9 잠정치 소스 대체 — KIS 종목별 가집계 (사용자 질의 후속, 2026-07-15 오후)**
- 문자의 수급(외인 현물·선물)은 **시장 단위**(KIS 시장별 투자자매매동향), L5·L9는 **하닉 종목
  단위** 잠정치 — 서로 다른 데이터. 종목 단위의 기존 소스(네이버 trend)는 장중 미제공 판명.
- 대체: **KIS 외국인·기관 매매종목가집계** (FHPTJ04400000, HTS [0440]) — 금액 기준 순매수 상위
  30 + 순매도 상위 30에서 하닉·삼전을 찾아 당일 추정 순매매량(주)을 틱에 적재 (실측: 상승일
  하닉 1위·삼전 2위. 부호는 매도 목록이 음수로 반환 — 그대로 사용. 양쪽 목록에 없으면 null).
- 이로써 L5(수준·감속)·L9(기관 흡수)가 장중 실시간 산출 — 재설계된 가점(+3·+1)에 반영.

**문자 정책 전면 개편 (사용자 지정 2026-07-15 오후)**
- **유지**: 아침브리핑 · 장중브리핑(체크포인트·전환·감속·판정변경·정기) · 판정 문자
  (확정 레버리지/인버스/V반등/V반등조기/횡보일·해제) · RV1 **강한 신호만**
  (변동폭 ≥ 임계값 × `strongRatio` 1.5 — 약한 성립은 판정·대시보드에만 반영, `strengthRatio` 기록).
- **중단** (`smsEnabled: false` — 감지·대시보드는 유지, 한 줄 재활성 가능): 삼전·하닉·K200선물
  급변/스윙 · 외인/프로그램 수급 반전 · 하닉 거래량 개별 문자.
- **장중브리핑에 거래량 라인 신설**: 하닉·삼전 최근 완성 5분봉 거래량 ÷ 당일 평균 —
  1.3배↑ 급증 / 0.6배↓ 급감 표기. 삼전 거래량 수집 시작 (마이그레이션 024 `samsung_vol`).

**진입가 적정성 줄 (사용자 지정 2026-07-15 저녁)**
- 실측 사고: 개장 +10% 진행 후 10시 판정·모멘텀 문자를 보고 추격매수 — 문자가 "신호가
  켜졌다"(사실)만 말하고 "지금 가격이 진입할 자리인가"(적정성)를 말하지 않았음.
- 판정 확정(레버리지/인버스/V반등/조기·약한추세 포함)·RV1 모멘텀 문자에 진입가 평가 줄 추가:
  하닉 당일 진행폭 ÷ ATR14 소진율 기준 —
  | 소진율 | 표기 |
  |---|---|
  | < 0.7배 | "초입 구간" |
  | 0.7~1.2배 | "중반 진행 — 신규는 1/3 이하·타이트 스탑" |
  | ≥ 1.2배 | **"추격 구간 — 신규 진입 비추·눌림/재형성 대기"** |
  ATR 부재 시 절대 %(4/8%) 폴백. 예: "진입가 주의: 하닉 당일 +11.2% = 통상 하루 변동(ATR)의
  1.3배 소진 — 추격 구간, 신규 진입 비추·눌림/재형성 대기".

**T4(외인 선물) 가중·임계 실측 보정 (사용자 지정 2026-07-15 저녁)**
- 실측 (7/14~15 KIS 수급 이틀, 틱 734개):
  - 누적 외인선물 ↔ FKS200 동행 상관: **추세일(7/15) 0.711 — 전 신호 최고 수준** / 왕복장(7/14) -0.150
  - Δ30분 부호 → 이후 30분 방향 적중률 52~57% — 선행(예측) 지표는 아님
  - |Δ30분| 분포: 중앙 ~1,300억 · 75% 1,836~3,062억 · 90% 2,173~4,173억
- 반영: **가중 3→4** (추세 '확인' 지표로 최고 대접 — fullMax 16→17),
  **임계 ±500 → ±1,500억** (기존 500은 25% 분위라 항상 충족 = 신호 아님. 75% 분위 하단 채택).
- 성격 규정: T4는 방향을 예측하지 않는다 — 형성된 추세에 외인 자금이 실리고 있는지를 확인한다.
  표본 축적 후 flowCalib 분위(0.75) 자동 보정으로 이관 예정.

### 2026-07-16 — 대규모 피드백 반영 (사용자 지정)

**발송·판정 운영**
- 조용일 7/17~18 (국장·미장): 판정 확정 문자(trend/vrebound/해제)만 예외, 전부 억제.
- 판정 구간 13:30 → **14:00** 연장. 전환·감속 브리핑 중단(판정 변경·체크포인트·정기만).
- 판정 확정 문자 **5분 지속 조건** (한국 — 미국 7/13과 동일). 역발상 라우팅은
  니케이·대만 중 **정합 시장이 하나도 없을 때만** (혼재=미상 — 13:19/13:39 왕복 수정).
- 브리핑: K200 지표 라인 전일比 통일 · 시초 2분 0.00% 가드 · "할 일" 액션 라인.

**리스크 자동화** — 스탑·트레일링 ATR 자동(0.7×/0.9×ATR×배수, 3~8% 클램프),
비중 = **총자산 10% 상한** 내 강도 비례 (추세일 강도3 10%/2 7%/1 5%, 약한추세 3%).

**갭(프리장) → 정규장 방향 통계 (45일 실측)** — 프리장 선판정 요청에 대한 검증:
- 하닉: 유의미 갭일 38일 중 방향 일치 **17일(45%)** / K200: 35일 중 19일(54%) — **동전던지기 수준**.
- 최근 불일치 다수: 7/9 갭+7.3→정규-1.9 · 7/10 갭+5→-5.1 · 7/15 갭+10.8→-1.8 · 7/14 갭-1.1→+4.8.
- 결론: **프리장(갭) 방향으로 조기 판정하면 안 된다** — 09:00~09:30 관찰 구간과 X1(갭 추격 금지)이
  데이터로 강하게 지지됨. 조기화 대신 T7(갭-초반 일치)이 09:30 직후 최속 확정을 담당.

**판정 성과 정량화** — 아침브리핑 ①에 어제 성과 자동 표기: 첫 판정 문자 시점 진입 가정
(상방=하닉 2배 / 하방=K200선물 -2배) → 15:00 청산 손익 % + 3천만원 기준 금액.

**7/8 사후 분석** — 사용자 질문("추세 상방 판정했을 것") 검증: 실제로는 **09:40 횡보일 선언
후 하루 종일 유지** — 추세일_상방 판정·레버리지 문자 없음, 손실 회피 성공. 하닉 갭-3.8% →
10시 +5.8% 급등 → 종일 하락 -5.2%: 스윙 산·골 반복으로 횡보일이 정확했음. 아쉬운 점은
13:30 이후 선물 급락(-4.8→-6.4)의 인버스 기회 — 판정 14:00 연장(금일 적용)이 이 구간을 커버.

**모바일 운영 설정·지시 (/ops, 마이그레이션 025)**
- 문자 일시정지(날짜까지·판정 문자 허용 옵션) — 발송 서버 60초 캐시로 즉시 적용.
- 자유 지시 저장함(ops_directives) — 다음 Claude 작업 세션에서 읽어 코드 반영 후 상태 갱신.

### 2026-07-16 (2차) — LM 매크로 게이트 개정: 상대 변화 → 절대 위험 레벨

- **기존 LM(매크로 악화 2Y↑·환율+0.3%↑·SOX-0.5%↓ 중 2개 이상 시 레버리지 금지) 적용 제외.**
  사용자: "매크로가 중요한 것은 맞다. 그러나 시장에서 감내할 수 있는 기준값이 계속 변경된다."
- **신규 LM: 절대 레벨 게이트** — 미 2년물 **≥ 4.2%** 또는 원달러 환율 **≥ 1,540원**이면
  위험 구간으로 보고 레버리지 진입 금지 (`config.macroGate`). 어느 하나만 넘어도 차단.
  레벨 미상(null)은 통과. V반등 조기 선진입 차단(decide.ts)에도 동일 적용(기존 연결 유지).
- 상대 변화식 매크로 악화(2/3)는 **S2 숏 가점에만 유지** — 게이트에서는 제거.
- (동일 개정 추가) **미 10년물 ≥ 4.6%**도 레버리지 금지 레벨에 포함 — 2Y·10Y·환율 중
  하나라도 위험 레벨이면 차단. 인버스(숏) 셋업은 LM의 차단 대상이 아님 (롱 전용 게이트).

### 2026-07-20 (2차) — X1 갭 임계 변동성 연동 (사용자 지적)

- "고정 +2%는 하닉의 최근 변동성 대비 너무 작다" — 실측 확인: ATR14 11.8% 장에서 220일 중
  29%(64일)가 갭업 2% 초과, 갭 |중앙값|조차 2.2%. 사흘에 하루 걸리는 임계는 '큰 갭' 판별 기능 상실.
- **개정: X1 실효 임계 = max(3%, 0.5×ATR14)** (`gapBigPct`=3 바닥 + `gapBigAtrRatio`=0.5,
  `x1GapThreshold()`). 현 변동성 기준 약 5.9%. 근거: 220일 실측에서 갭 0.5~0.8×ATR 구간이
  시초 추격 성적 최악(평균 -0.64%·승률 38%), 고정 구간별로는 유의한 악화 없음.
- 적용: 셋업 X1 · V반등 조기 선진입 가드 · 예측 M7근사 모델 동일. 회귀 54건 통과.
- 참고 관찰(소표본 27일): 갭 0.8×ATR 초과의 극단 갭은 오히려 지속 경향(+0.53%·승률 59%) —
  브레이크어웨이 갭 가설. 표본 부족으로 규칙화하지 않고 기록만.

### 2026-07-21 — 미장 프리장(프리마켓) 판정 (사용자 지정)

한국 애프터장 판정(예측 시스템)의 미국판 — SMH 프리마켓 가격행동으로 정규장 방향을 선판정한다.
부록 B의 "프리장 **갭 방향** 조기판정 금지"와 모순 아님: 갭 부호 단독이 아니라 프리마켓
**가격행동(피셔 ACD)**을 쓴다. 미국 실측에서도 갭 방향 단독은 43~46%(동전던지기)로 재확인.

**구조** (`lib/signal/us/premarket.ts` · `US_SIGNAL_CONFIG.premarket` · 마이그레이션 029)
- 세션: SMH 프리마켓 5분봉(야후 includePrePost), **시작 07:00 ET** — OR 07:00~07:30(6봉),
  A확인 5분봉 2개 연속(10분), C철회 1봉. 04:00 시작은 박봉·역예측으로 배제 (실측 적중 28~38%).
- 체크포인트 08:00 → 08:30 → 09:00 → **확정 09:25 ET(개장 직전, KST 22:25)**. 사이 구간
  모니터링(변경 시 문자). 크론이 22:25에 시작해도 지난 컷을 소급 판정해 개장 전 확정 문자는 나감
  — 실시간 커버는 크론 시작을 **20:55 KST**(겨울 21:55)로 당기면 된다.
- 오프셋 = 세션 시가 % — **조기 컷(08:00·08:30) 0.15% / 후기 컷(09:00·09:25) 0.4%**.
  한국 정규장의 "이른 시각 낮은 문턱(기회)·늦은 시각 높은 문턱(확인)" 구조가 미국에서도 실측 재현.
- 채점(정규장 마감 후 자동 백필): 정규장 시가 진입 가정 — 시가→종가 부호 적중 + 스탑
  (SMH -1.5% ≈ 2x ETF -3%) 적용 손익. `/signal/us` 페이지에 판정·누적 표기.

**백테스트 근거** (scripts/us-premarket-backtest.ts — 38거래일 5분봉, 세션시작 4×OR 2×오프셋 7×확인 2×컷 4 스윕)
| 컷 (ET) | 채택 조합 성적 | 갭 대조군 |
|---|---|---|
| 08:30 | 17신호 · 적중 59% · 스탑누적 +14.3%p | 43% · -0.2%p |
| 09:00 | 16신호 · 50% · +9.7%p | 43% |
| 09:25 | 18신호 · 50% · +6.7%p | 43% |
- 전·후반 분할: 컷 08:30은 63%/56%로 재현. 후반(갭 대조군 37%·-6.7%p로 붕괴)에도 피셔는 손익 방어.
- ⚠ **소표본(38일)·스윕 선택 편향 내재** — 문자에 "소액만" 경고 동봉, 라이브 채점 누적으로 재검증
  (섹터 트래킹과 동일 원칙). 야후 5분봉 60일 한계로 표본 확장은 라이브 축적뿐.
